---
phase: 16-live-ui-indicators
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/ui/panels.rs
autonomous: false
requirements: [UI-01, UI-02, UI-03]

must_haves:
  truths:
    - "The worktree row renders Y and P as two independent cells separated by a space (no slash, no merged Y/P string)"
    - "While a YarnInstall task runs for a worktree, its Y cell shows a yellow rotating spinner; the P cell still shows its static letter"
    - "While a YarnPodInstall task runs, its P cell shows a yellow rotating spinner; the Y cell still shows its static letter"
    - "Every other running task shows a yellow spinner + short label + live elapsed in the new rightmost task column"
    - "The elapsed counter advances on each 250ms tick with no mutable tick/frame field added to AppState"
    - "Columns stay aligned across all 6 spinner frames in the target terminal"
  artifacts:
    - path: "src/ui/panels.rs"
      provides: "render_worktree_table with split Y/P cells, task column, spinner + elapsed wiring"
      contains: "task_short_label"
  key_links:
    - from: "src/ui/panels.rs"
      to: "crate::ui::indicators::{spinner_frame, format_elapsed, task_short_label}"
      via: "use import of the Plan 16-01 helpers"
      pattern: "use crate::ui::indicators"
    - from: "src/ui/panels.rs"
      to: "crate::app::state::task_for_worktree"
      via: "per-row lookup inside the worktree render loop"
      pattern: "task_for_worktree\\(state, &wt\\.id\\)"
---

<objective>
Wire the Plan 16-01 helpers into `render_worktree_table` (`src/ui/panels.rs`) so the worktree table reflects live per-worktree task state every frame. Three coordinated changes to one file: (1) split the merged `Y/P` icon span into independent `Y` cell, space separator, `P` cell, each showing a yellow spinner only when its own install task is running (UI-01); (2) add a new rightmost task column showing `◐ <label> <elapsed>` for any other running task (UI-02); (3) compute elapsed live from `record.started_at.elapsed()` in the render path with zero new `AppState` state (UI-03). The 250ms tick at `runtime.rs:31` already drives redraw, so live animation is free.

Because half-circle glyph width cannot be verified by unit tests, this plan ends with a blocking human-verify checkpoint that confirms column alignment in the real terminal and exercises the one-line `SPINNER_FRAMES` fallback path if needed.

Purpose: Delivers UI-01 (split cells), UI-02 (spinner in the right place per task category), and UI-03 (live no-state elapsed). This is the user-visible payoff of the phase.

Output: Modified `src/ui/panels.rs` (split icon cell, task column, fixed Constraint array, fixed metro detail row).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/16-live-ui-indicators/16-CONTEXT.md
@.planning/phases/16-live-ui-indicators/16-RESEARCH.md
@.planning/phases/16-live-ui-indicators/16-PATTERNS.md
@.planning/phases/16-live-ui-indicators/16-VALIDATION.md

@src/ui/panels.rs

<interfaces>
<!-- Contracts CONSUMED from Plan 16-01 (src/ui/indicators.rs). Use directly — no exploration. -->
```rust
pub const SPINNER_FRAMES: [&str; 6];
pub fn spinner_frame(elapsed: std::time::Duration) -> &'static str;   // yellow glyph for current 150ms window
pub fn format_elapsed(elapsed: std::time::Duration) -> String;        // "42s" | "1:34" | "12:03"
pub fn task_short_label(spec: &crate::domain::command::CommandSpec) -> &'static str;
```

<!-- Read path the row loop uses (verified in source) -->
From src/app/state.rs:290:
```rust
pub fn task_for_worktree<'a>(state: &'a AppState, id: &WorktreeId) -> Option<&'a TaskRecord>;
```
From src/domain/task.rs:45-48 (read-only):
```rust
pub struct TaskRecord { pub id: TaskId, pub spec: CommandSpec, pub started_at: Instant, /* handle */ }
```
`task_for_worktree` is already called in this file in `render_command_output` (panels.rs:207-210). `wt.id: WorktreeId` (worktree.rs:24). `Option<&TaskRecord>` is `Copy` — `if let Some(record) = task` does not move it, so both the Y arm and P arm can re-use `task`.
</interfaces>

<line_pins>
<!-- Verify these before editing — confirmed at planning time -->
- panels.rs:82 — comment `// Status icons:` then `let mut icon_spans: Vec<Span> = Vec::new();`
- panels.rs:86-90 — metro indicator spans (UNCHANGED)
- panels.rs:92-94 — yarn `Y` span (gets split logic)
- panels.rs:96-98 — pods `/P` span (becomes space + `P`)
- panels.rs:114-119 — `rows.push(Row::new(vec![...]))` — 4-cell version → 5 cells
- panels.rs:123-137 — metro detail row — 4-cell version → 5 cells
- panels.rs:156-164 — `Table::new(rows, [Length(8), Length(20), Min(20), Length(16)])` → append 5th column
</line_pins>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split Y/P into independent cells with per-install spinner</name>
  <files>src/ui/panels.rs</files>
  <read_first>
    - src/ui/panels.rs lines 82-120 (current icon span construction + Row::new — the exact block being replaced)
    - .planning/phases/16-live-ui-indicators/16-PATTERNS.md §"Current icon span construction" and §"Note on borrow" (Option<&TaskRecord> is Copy)
    - .planning/phases/16-live-ui-indicators/16-RESEARCH.md §"Pitfall 2" (destructure task once; no double-borrow) and §"Pitfall 4" (call task_for_worktree only inside the row loop, before render_stateful_widget)
    - .planning/phases/16-live-ui-indicators/16-CONTEXT.md D-02 (Y=YarnInstall only, P=YarnPodInstall only), D-03 (drop slash → `Y P`), D-06/D-09 (yellow spinner, staleness hidden while spinning), D-10 (no clear logic; recompute staleness next frame)
  </read_first>
  <behavior>
    - When `task_for_worktree(state, &wt.id)` is `None`: Y cell is `Y` colored red if `wt.stale` else green; separator is a single space; P cell is `P` colored red if `wt.stale_pods` else green. (No slash anywhere.)
    - When the running task spec is `YarnInstall`: Y cell shows `spinner_frame(record.started_at.elapsed())` styled yellow; P cell still shows its static letter+staleness color.
    - When the running task spec is `YarnPodInstall`: P cell shows the yellow spinner; Y cell still shows its static letter+staleness color.
    - When the running task is any other variant: both Y and P show static letters+staleness (the spinner for that task lives in the task column, added in Task 2).
    - Icon column total visual width stays 5 chars (`  ` + 1 + 1 + 1), so `Constraint::Length(8)` is unchanged.
  </behavior>
  <action>
    Add `use crate::ui::indicators::{spinner_frame, format_elapsed, task_short_label};` to the imports near the existing `crate::ui::theme` import (format_elapsed/task_short_label are used in Task 2 — import all three now).

    Inside the `for wt in ...` row loop, immediately after the `let mut icon_spans` line, fetch the running task once: `let task = crate::app::state::task_for_worktree(state, &wt.id);` (same accessor already used at panels.rs:210). Keep the metro indicator block (lines 86-90) UNCHANGED.

    Replace the yarn `Y` span (lines 92-94) with the D-02/D-09 branch: if `task` is `Some(record)` and `matches!(&record.spec, CommandSpec::YarnInstall)`, push `Span::styled(spinner_frame(record.started_at.elapsed()), Style::default().fg(Color::Yellow))`; otherwise push the existing static `Y` span colored by `wt.stale` (red) / else green. Reference `CommandSpec` via `crate::domain::command::CommandSpec`.

    Replace the pods `/P` span (lines 96-98) with: push `Span::raw(" ")` as the separator (D-03 — replaces the slash), then the P branch: if `task` is `Some(record)` and `matches!(&record.spec, CommandSpec::YarnPodInstall)`, push the yellow spinner span; otherwise push the static `P` span colored by `wt.stale_pods`. Note the static text is now `"P"` (not `"/P"`).

    Do not add any AppState field; the spinner index comes entirely from `record.started_at.elapsed()`. Update the inline comment `// Status icons: ... Y and /P` to drop the slash. Per Pitfall 4, keep `task_for_worktree` calls inside the loop only — do not hoist near the later `render_stateful_widget` call.
  </action>
  <verify>
    <automated>cargo build --incremental 2>&1 | tail -15 && cargo test --all-targets 2>&1 | grep -E 'FAILED|error\[' || echo "no failures"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `! rg '"/P"' src/ui/panels.rs` (the merged slash form is gone — UI-01).
    - Source assertion: `rg -c 'task_for_worktree\(state, &wt\.id\)' src/ui/panels.rs` ≥ 1 (per-row task lookup added).
    - Source assertion: `rg 'CommandSpec::YarnInstall' src/ui/panels.rs` and `rg 'CommandSpec::YarnPodInstall' src/ui/panels.rs` each match (Y and P gated to their own variant only — D-02).
    - Source assertion: `rg 'Color::Yellow' src/ui/panels.rs` matches in the icon block (D-06).
    - No new `AppState` field: `git diff src/app/` is empty (only panels.rs changes).
    - `cargo build --incremental` compiles; `cargo test --all-targets` shows no FAILED / no `error[`.
    - `make arch-lint` exit 0 (G-02 still green — only domain/ui imports added).
  </acceptance_criteria>
  <done>Y and P render as independent cells separated by a space; YarnInstall spins the Y cell yellow, YarnPodInstall spins the P cell yellow, the other cell stays static; no slash, no AppState field, build + tests + arch-lint green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add task column + fix Constraint array and metro detail row</name>
  <files>src/ui/panels.rs</files>
  <read_first>
    - src/ui/panels.rs lines 114-164 (Row::new main row, metro detail row, Table::new Constraint array — all three need a 5th cell/column)
    - .planning/phases/16-live-ui-indicators/16-PATTERNS.md §"Task cell construction", §"Row::new ... 5-cell version", §"Metro detail row ... add 5th cell", §"Table::new Constraint array ... 5-column version"
    - .planning/phases/16-live-ui-indicators/16-RESEARCH.md §"Column Width Analysis" (longest cell `◐ unit-tests 12:03` = 18 chars → Length(20)) and §"Pitfall 1" (every Row must have the same cell count as the Constraint array)
    - .planning/phases/16-live-ui-indicators/16-CONTEXT.md D-04 (task col = every running task NOT YarnInstall/YarnPodInstall; empty when idle), D-07 (labels)
  </read_first>
  <behavior>
    - When `task` is `Some(record)` and the spec is NOT `YarnInstall` and NOT `YarnPodInstall`: the task cell string is `format!("{} {} {}", spinner_frame(elapsed), task_short_label(&record.spec), format_elapsed(elapsed))` where `elapsed = record.started_at.elapsed()` (e.g. `◐ jest 1:34`).
    - When `task` is `None`, or the spec IS `YarnInstall`/`YarnPodInstall` (those animate in the Y/P cells, never the task column): the task cell is an empty `String`.
    - The main worktree row has exactly 5 cells; the metro detail row has exactly 5 cells; the Constraint array has exactly 5 columns. Cell counts match the constraint count (no ratatui row/column mismatch).
    - The task-column elapsed advances every 250ms tick because it is recomputed from `started_at.elapsed()` each render — no stored state.
  </behavior>
  <action>
    Before the `rows.push(Row::new(...))` call, build `let task_cell: String` per D-04: `match task { Some(record) if !matches!(&record.spec, CommandSpec::YarnInstall | CommandSpec::YarnPodInstall) => { let elapsed = record.started_at.elapsed(); format!("{} {} {}", spinner_frame(elapsed), task_short_label(&record.spec), format_elapsed(elapsed)) } _ => String::new() }`. This reuses the `task` binding from Task 1.

    Add `Cell::from(task_cell)` as the 5th cell of the main `Row::new(vec![...])` (panels.rs:114-119), after the existing dir-name cell.

    Add a 5th `Cell::from("")` to the metro detail row `Row::new(vec![...])` (panels.rs:123-137) — per Pitfall 1, every Row must have the same number of cells as there are constraints, or ratatui misaligns/panics. The detail row's task column is always empty.

    Append a 5th constraint to the `Table::new` array (panels.rs:156-164): `Constraint::Length(20), // Task (◐ unit-tests 12:03 = 18 chars + 2 margin)`. Leave the existing four constraints unchanged (icon `Length(8)` width is correct — see RESEARCH Column Width Analysis). Update the icon-column comment from `Y + /P` to `Y + P`.
  </action>
  <verify>
    <automated>cargo build --incremental 2>&1 | tail -15 && cargo test --all-targets 2>&1 | grep -E 'FAILED|error\[' || echo "no failures"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `rg -c 'Cell::from' src/ui/panels.rs` increased by the new cells; both the main row and the detail row now have 5 `Cell::from(...)` entries each (manual count of each `Row::new` block).
    - Source assertion: the `Table::new` constraint array has 5 entries — `rg -A8 'Table::new\(' src/ui/panels.rs` shows 5 `Constraint::` lines including the new `Length(20)` task column.
    - Source assertion: `rg 'task_short_label' src/ui/panels.rs` and `rg 'format_elapsed' src/ui/panels.rs` each match (task cell wires both helpers — D-04).
    - Behavior: a YarnInstall/YarnPodInstall task yields an empty task cell (string-equality reasoning — those animate in Y/P, gated by the `!matches!(... YarnInstall | YarnPodInstall)` guard).
    - `cargo build --incremental` compiles; `cargo test --all-targets` shows no FAILED / no `error[`; `make arch-lint` exit 0.
    - No new `AppState` field: `git diff src/app/` empty.
  </acceptance_criteria>
  <done>The task column shows `◐ <label> <elapsed>` for non-install running tasks and is empty otherwise; main row, detail row, and constraint array all have 5 entries; build + tests + arch-lint green; no AppState change.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human terminal-alignment + live-animation verification</name>
  <what-built>
    Plans 16-01 + 16-02 implemented the live worktree-table indicators: split independent Y and P cells, a 6-frame yellow half-circle spinner (`SPINNER_FRAMES = ◐ ◓ ◑ ◒ ◐ ◓`) that replaces the Y cell during a yarn install, the P cell during a pod install, and appears with a short label + live elapsed in a new rightmost task column for every other running task. Elapsed is recomputed from `started_at.elapsed()` every 250ms tick with no new AppState field. All pure helpers are unit-tested; the one thing unit tests cannot prove is terminal glyph cell width.
  </what-built>
  <how-to-verify>
    1. Build and run in the target terminal (tmux + iTerm2 per CLAUDE.md): `cargo build --incremental && cargo run`.
    2. Confirm each worktree row shows `Y P` (Y, single space, P) with NO slash, and Y/P colors still reflect staleness (red=stale, green=fresh) when idle.
    3. Trigger a yarn install on one worktree → its Y cell becomes a rotating yellow spinner cycling `◐ ◓ ◑ ◒`; the P cell stays a static letter. The Y/P columns and all columns to the right MUST stay vertically aligned across every animation frame.
    4. Trigger a pod install on another worktree → its P cell spins yellow; Y stays static; alignment holds.
    5. Trigger a jest/lint/run-ios task → the rightmost task column shows `◐ <label> <elapsed>` (e.g. `◐ jest 1:34`); confirm the elapsed counter visibly advances on the 250ms tick and the format matches D-08 (`5s`, `42s`, `1:34`, `12:03`).
    6. Let a task finish → the spinner disappears and the cell returns to its static letter / empty task column.
    7. ALIGNMENT CHECK (the real risk): if the half-circle glyphs render double-width and push columns out of alignment in any frame, this fails. Fix by swapping the single line `pub const SPINNER_FRAMES` in `src/ui/indicators.rs` to the braille set `["⠋","⠙","⠹","⠸","⠼","⠴"]` (all narrow) — no other change — then rebuild and re-verify. Update the inline boundary tests' expected glyphs if you swap (the index→glyph mapping changes).
  </how-to-verify>
  <resume-signal>Type "approved" when columns stay aligned across all spinner frames and elapsed advances live; or describe the misalignment / which fallback set you applied.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Render-only modification of `render_worktree_table`. Reads existing trusted in-memory `AppState` (`Duration`, `CommandSpec`, staleness flags) and emits ratatui spans. No new I/O, network, persistence, auth, or user-supplied input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | (none) | src/ui/panels.rs render_worktree_table | accept | No high/medium threats. Render-only over trusted in-process state; no I/O, network, persistence, auth, or external input introduced. The only correctness risk (glyph cell-width alignment) is a display concern, not a security threat, and is covered by the blocking human-verify checkpoint with a documented single-line fallback. No new dependencies. |

No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages — this plan adds zero dependencies, so no Package Legitimacy / `T-16-SC` checkpoint applies.
</threat_model>

<verification>
- `cargo build --incremental` — compiles.
- `cargo test --all-targets 2>&1 | grep -E 'FAILED|error'` — no regressions (existing tests stay green; pure-helper tests from 16-01 stay green).
- `make arch-lint` — exit 0 (G-02: `ui/` imports zero infra; only domain/ui imports added).
- `git diff src/app/ src/domain/ src/infra/` — empty (render-only; no state, domain, or infra change).
- Human-verify checkpoint — column alignment across all 6 spinner frames + live elapsed advance confirmed in target terminal.
</verification>

<success_criteria>
- UI-01: Y and P render as two independent cells (space-separated, no slash); each can show its letter or a spinner independently.
- UI-02: YarnInstall spins the Y cell yellow; YarnPodInstall spins the P cell yellow; every other running task shows `◐ <label> <elapsed>` in the task column; all return to static when idle.
- UI-03: elapsed is computed live from `started_at.elapsed()` each render and advances on the 250ms tick — verified `git diff src/app/` empty (no mutable tick/frame field).
- `cargo test --all-targets` green; `make arch-lint` PASS; terminal alignment confirmed.
</success_criteria>

<output>
Create `.planning/phases/16-live-ui-indicators/16-02-SUMMARY.md` when done.
</output>
