---
phase: 13-audit-driven-refactors
plan: 06
type: execute
wave: 4
depends_on: [13-01, 13-02, 13-03, 13-04, 13-05]
files_modified:
  - src/app/mod.rs
  - src/app/state.rs
  - src/app/update.rs
  - src/app/handle_key.rs
  - src/app/runtime.rs
  - src/app/effect_runner.rs
  - src/app/adapters.rs
  - src/app/dispatch_tests.rs
  - src/app.rs
  - src/lib.rs
  - src/main.rs
autonomous: true
requirements: [REFACTOR-01]
requirements_addressed: [REFACTOR-01]
tags: [refactor, app-split, structural-lift, F-200, wave-c]

must_haves:
  truths:
    - "src/app.rs no longer exists at repo root — replaced by src/app/ directory (F-200 structural split)"
    - "src/app/mod.rs declares all submodules and re-exports the public API (AppState, FocusedPanel, PaletteMode, ErrorState, active_worktree_id, update, run, handle_key) so rn_dash::app::* paths stay stable"
    - "src/app/dispatch_tests.rs module declaration survives the split — `#[cfg(test)] pub mod dispatch_tests;` in src/app/mod.rs (Pitfall 1)"
    - "All 17 COVER-03 dispatch tests still discoverable and passing after the split"
    - "cargo test --all-targets still exits 0 (49+ tests)"
    - "cargo clippy --all-targets -- -D warnings exits 0"
    - "NO behavior change — update() signature is unchanged (F-201 consumer is Plan 13-07)"
    - "make cov-check still produces valid output (coverage thresholds updated for the structural split per Pitfall 7)"
    - "src/main.rs still does `use rn_dash::{app, tui}` — module root API preserved"
  artifacts:
    - path: "src/app/mod.rs"
      provides: "Module index + re-exports for the 7 submodules + dispatch_tests"
      contains: "pub mod state"
    - path: "src/app/state.rs"
      provides: "AppState struct + FocusedPanel + PaletteMode + ErrorState + helpers (active_worktree_id/active_output/active_output_scroll)"
      contains: "pub struct AppState"
    - path: "src/app/update.rs"
      provides: "pub fn update (signature UNCHANGED — body moves verbatim)"
      contains: "pub fn update"
    - path: "src/app/handle_key.rs"
      provides: "pub fn handle_key (body moves verbatim; F-208 consumer is Plan 13-07)"
      contains: "pub fn handle_key"
    - path: "src/app/runtime.rs"
      provides: "pub async fn run (event loop; also temporarily hosts the 7 async metro helpers until Plan 13-07 moves them to infra/metro.rs)"
      contains: "pub async fn run"
    - path: "src/app/effect_runner.rs"
      provides: "STUB — empty struct + empty impl; Plan 13-08 populates"
      contains: "pub struct EffectRunner"
    - path: "src/app/adapters.rs"
      provides: "STUB — empty struct; Plan 13-08 populates"
      contains: "pub struct Adapters"
    - path: "src/app/dispatch_tests.rs"
      provides: "PRESERVED 17 COVER-03 tests — the file MUST continue to compile after the split"
      contains: "#[cfg(test)]"
  key_links:
    - from: "src/app/mod.rs"
      to: "src/app/dispatch_tests.rs"
      via: "#[cfg(test)] pub mod dispatch_tests;"
      pattern: "#\\[cfg\\(test\\)\\] pub mod dispatch_tests"
    - from: "src/lib.rs"
      to: "src/app/mod.rs"
      via: "`pub mod app;` — points to the directory after the split"
      pattern: "pub mod app"
    - from: "src/main.rs"
      to: "rn_dash::app::run"
      via: "unchanged import path"
      pattern: "use rn_dash::.*app"

threat_model_disposition: accept_refactor_only
# Pure structural lift-and-shift. Zero behavior change. All 49 existing tests guard
# the invariants that must survive. No new trust boundaries.
---

<objective>
Implement F-200 (Critical): split the 2,428-line src/app.rs into 7 files under src/app/:
mod, state, update, handle_key, runtime, effect_runner (stub), adapters (stub). This is
a STRUCTURAL LIFT — no behavior change. The update() signature stays unchanged (still
takes `&mut AppState, Action, metro_tx, handle_tx`). The F-201 consumer rewrite that
changes update() to return `Vec<Effect>` is Plan 13-07.

Purpose: F-201 (TEA purity), F-202 (hexagonal injection), F-203 (metro helpers move),
F-208 (KEYBINDINGS consumer), F-204 (Recipe consumer), F-205 (exhaustive modal arms),
F-209 (AppState sub-structs) all need the new file layout to exist before they can be
applied. The split itself carries risk (2500 lines moved across 6 files; forgetting
one re-export silently breaks compilation or drops tests from the build) — hence it
gets a dedicated plan.

**CRITICAL requirement (Pitfall 1):** `src/app/dispatch_tests.rs` sub-module must keep
compiling. The `#[cfg(test)] pub mod dispatch_tests;` declaration that currently lives
at `src/app.rs:2427-2428` MUST migrate to `src/app/mod.rs` in the same commit — otherwise
the 17 COVER-03 tests disappear from the build silently.

Output: 7 new files in src/app/; src/app.rs deleted; dispatch_tests.rs re-declared in
mod.rs; all 49 tests still pass; clippy clean; coverage thresholds updated (old
`src/app.rs` row deleted, new rows for 6 of the new files added per floor-to-5 policy).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-architecture-audit/AUDIT.md
@.planning/phases/13-audit-driven-refactors/13-RESEARCH.md
@.planning/phases/13-audit-driven-refactors/13-PATTERNS.md
@.planning/phases/13-audit-driven-refactors/13-01-SUMMARY.md
@.planning/phases/13-audit-driven-refactors/13-05-SUMMARY.md

<interfaces>
<!-- The source file (src/app.rs) has very specific line ranges that map to destination
     files. Executor uses `wc -l src/app.rs` and `grep -n` to find exact boundaries
     before moving. -->

From AUDIT.md F-200 (lines 355-377) — target file layout:
```
src/app/
├── mod.rs            — re-exports
├── state.rs          — AppState + Default + FocusedPanel + ErrorState + helpers (from src/app.rs:1-256)
├── update.rs         — pub fn update + pub fn dispatch_command (from src/app.rs:485-2061)
├── effect_runner.rs  — STUB (populated in Plan 13-08)
├── handle_key.rs     — pub fn handle_key (from src/app.rs:258-478)
└── runtime.rs        — pub async fn run + 7 async metro helpers (from src/app.rs:2063-2425)
                        (Plan 13-07 moves the 7 metro helpers OUT to src/infra/metro.rs)
```

From 13-PATTERNS.md:696-728 — src/app/mod.rs template:
```rust
//! App layer — TEA event loop, state mutation, effect interpretation.
pub mod adapters;
pub mod effect;         // already exists from Plan 13-03
pub mod effect_runner;
pub mod handle_key;
pub mod runtime;
pub mod state;
pub mod update;
// keybindings.rs arrives in Plan 13-07 — NOT in this plan's mod list

// Re-exports for tests and ui/ — KEEPS rn_dash::app::* paths stable
pub use state::{
    active_output, active_output_scroll, active_worktree_id,
    AppState, ErrorState, FocusedPanel, PaletteMode,
};
pub use update::update;
pub use runtime::run;
pub use handle_key::handle_key;

#[cfg(test)]
pub mod dispatch_tests;
```

From src/app.rs (current 2428 lines) — line-range map for the split:
  Lines 1-256: AppState definition, FocusedPanel, ErrorState, PaletteMode, Default
    impl, helpers active_worktree_id/active_output/active_output_scroll
    → moves to src/app/state.rs (verbatim)
  Lines 258-478: pub fn handle_key
    → moves to src/app/handle_key.rs (verbatim)
  Lines 485-536: pub fn dispatch_command
    → moves to src/app/update.rs (alongside update; some research notes keep it a
    sibling fn in update.rs)
  Lines 538-2061: pub fn update
    → moves to src/app/update.rs (verbatim — signature unchanged)
  Lines 2063-2202: pub async fn run
    → moves to src/app/runtime.rs (verbatim)
  Lines 2209-2425: 7 async metro helpers (spawn_metro_task, metro_process_task,
    parse_metro_line, extract_percent, drain_metro_output, stdin_writer, metro_http_post)
    → moves to src/app/runtime.rs (Plan 13-07 moves these OUT to infra/metro.rs)
  Lines 2427-2428: `#[cfg(test)] mod dispatch_tests;`
    → migrates to src/app/mod.rs as `#[cfg(test)] pub mod dispatch_tests;` (pub
    to allow cross-module test utility sharing)

**CRITICAL:** The InAppMetroHandle struct added in Plan 13-03 (near the metro helpers,
likely bottom of src/app.rs) MUST come with the metro helpers into runtime.rs
temporarily. Plan 13-07 moves it out with the 7 metro helpers.

**STUB files:**
- src/app/effect_runner.rs: `//! Plan 13-08 populates. \npub struct EffectRunner;`
  (minimal skeleton; the full impl lands in 13-08)
- src/app/adapters.rs: `//! Plan 13-08 populates. \npub struct Adapters;`

Plan 13-03 already created src/app/effect.rs (Effect enum). That file stays — it's a
legitimate submodule and is already listed in src/app/mod.rs template.

**use-statement management:** src/app.rs has many `use` statements at the top. These
distribute across the destination files — each destination imports only what its
contents need. Rust compiler drives: after move, `cargo check` reports unused imports
per file; prune to match what the destination actually uses.

Common imports that go in every file:
- `use crate::domain::action::Action;` → handle_key.rs, update.rs
- `use crate::domain::command::*;` → update.rs, state.rs, handle_key.rs (whichever
  types each uses)
- `use ratatui::crossterm::event::*;` → handle_key.rs, runtime.rs

From src/lib.rs (post-13-01 state):
```rust
// Before this plan:
pub mod app;  // points to src/app.rs
pub mod domain;
pub mod event;
pub mod infra;
pub mod tui;
pub mod ui;
```
After this plan: `pub mod app;` now resolves to `src/app/mod.rs` (directory module).
No other change needed at lib.rs.

From src/main.rs:
```rust
use rn_dash::{app, tui};
// or
use rn_dash::app::run;
// main() calls app::run(terminal)
```
After this plan: unchanged — rn_dash::app::run still resolves via the re-export in
src/app/mod.rs.

**Pitfall 7 — coverage thresholds update procedure:**
- `.planning/phases/12-coverage-gate/COVERAGE-THRESHOLDS.md` currently has a row for
  `src/app.rs` with its floor(baseline, 5) threshold
- After the split: DELETE the `src/app.rs` row; ADD new rows for:
    - `src/app/mod.rs` (re-exports only — likely 0% coverage; floor-to-5 → 0)
    - `src/app/state.rs` (Default impl + helpers; some coverage from existing tests
      that touch AppState::default())
    - `src/app/update.rs` (bulk of coverage — inherits most of the src/app.rs coverage
      since dispatch_tests exercises it)
    - `src/app/handle_key.rs` (tests via dispatch_tests palette resolution)
    - `src/app/runtime.rs` (0% — event loop untested in unit tests)
    - `src/app/effect_runner.rs` (0% — stub)
    - `src/app/adapters.rs` (0% — stub)
- Changelog entry: "2026-04-24: Phase 13 Plan 13-06 — F-200 app.rs split; new per-file
  rows; floor-to-5 policy applies."

**Rust module resolution note:** After the split, `src/app.rs` and `src/app/mod.rs`
CANNOT both exist — rustc will error. Delete src/app.rs once src/app/mod.rs is in
place. BUT: Plan 13-03 already added `pub mod effect;` to src/app.rs and created
src/app/effect.rs (this works because `src/app.rs` with submodule decls DOES resolve
`src/app/<sub>.rs` sibling files under 2018+ edition). This plan must:
1. Create src/app/mod.rs with all submodule declarations
2. Move src/app.rs contents into the appropriate sibling files
3. Delete src/app.rs
4. Verify cargo build
</interfaces>

<external_references>
- AUDIT.md F-200 (lines 355-377) — the god-object split with file-level D-04 sketch
- Refactor Sequence Group C item 15 (AUDIT.md:876-880) — sequencing rationale
- 13-RESEARCH.md §Recommended Target Plans 13-06 (lines 815-829) — structural lift guidance
- 13-RESEARCH.md Pitfall 1 (lines 937-941) — dispatch_tests module declaration migration
- 13-RESEARCH.md Pitfall 7 (lines 973-983) — coverage threshold rows for split
- 13-PATTERNS.md §src/app/mod.rs (lines 696-728) — exact mod.rs skeleton
- 13-PATTERNS.md §src/app/state.rs (lines 730-793) — state lift (Phase A: verbatim; Phase B: sub-struct grouping is Plan 13-10)
- 13-PATTERNS.md §src/app/update.rs (lines 795-858) — update lift (Phase A verbatim; Phase B F-201 rewrite is Plan 13-07)
- 13-PATTERNS.md §src/app/handle_key.rs (lines 931-962)
- 13-PATTERNS.md §src/app/runtime.rs (lines 964-1022)
</external_references>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Split src/app.rs into src/app/{mod,state,update,handle_key,runtime,effect_runner,adapters}.rs — verbatim lift, no behavior change</name>
  <files>
    src/app/mod.rs (NEW),
    src/app/state.rs (NEW — carved from src/app.rs:1-256),
    src/app/update.rs (NEW — carved from src/app.rs:485-2061),
    src/app/handle_key.rs (NEW — carved from src/app.rs:258-478),
    src/app/runtime.rs (NEW — carved from src/app.rs:2063-2425 including 7 metro helpers + InAppMetroHandle bridge),
    src/app/effect_runner.rs (NEW — STUB),
    src/app/adapters.rs (NEW — STUB),
    src/app.rs (DELETE — after content migrated),
    src/lib.rs (unchanged — `pub mod app;` now resolves to directory),
    src/main.rs (unchanged — rn_dash::app::* paths still resolve)
  </files>
  <read_first>
    - src/app.rs (FULL FILE — 2428 lines; executor reads the entire file to verify line ranges match AUDIT F-200 sketch)
    - src/app/effect.rs (existing — Plan 13-03 created; not touched in this plan but mod.rs references it)
    - src/lib.rs (to verify `pub mod app;` is present and nothing else needs to change)
    - src/main.rs (to verify how the executable uses rn_dash::app::*)
    - tests/metro_single_instance.rs (imports `rn_dash::app::update`; MUST continue to resolve)
    - tests/common/mod.rs (imports `rn_dash::domain::ports::metro_port::MetroHandle`; unaffected)
    - src/app/dispatch_tests.rs if it exists as a separate file already (Phase 12 created this per STATE.md decisions)
    - .planning/phases/11-architecture-audit/AUDIT.md lines 355-377 (F-200)
    - .planning/phases/13-audit-driven-refactors/13-PATTERNS.md lines 696-1022 (all the app/* analogs)
    - .planning/phases/12-coverage-gate/COVERAGE-THRESHOLDS.md (row for src/app.rs to update)
  </read_first>
  <action>
    This is HIGH RISK (2500 LOC moved across 6 files). Work one file at a time. After
    each file creation, `cargo build` MUST exit 0. If it doesn't, fix imports before
    moving to the next.

    STEP 1 — Check Plan 13-03's module resolution. If src/app.rs currently has
    `pub mod effect;` and src/app/effect.rs exists, verify `cargo build` still works.
    If yes, good. If not, this plan might need to invert the order — create src/app/mod.rs
    FIRST, then move everything else.

    STEP 2 — Identify exact line boundaries in the current src/app.rs using grep:
    - `grep -n '^pub fn handle_key' src/app.rs` — start of handle_key block
    - `grep -n '^pub fn update' src/app.rs` — start of update block
    - `grep -n '^pub fn dispatch_command' src/app.rs` — start of dispatch_command
    - `grep -n '^pub async fn run' src/app.rs` — start of runtime::run
    - `grep -n '^async fn spawn_metro_task\|^fn parse_metro_line\|^async fn metro_process_task' src/app.rs` — metro helpers start
    - `grep -n '#\[cfg(test)\] mod dispatch_tests' src/app.rs` — module declaration line
    - Plan 13-03's `pub mod effect;` line — already added to top of src/app.rs

    Verify the line numbers match what AUDIT F-200 documents (1-256, 258-478, 485-536,
    538-2061, 2063-2202, 2209-2425, 2427-2428). If they differ by a few lines due to
    Plan 13-01's import rewrites + Plan 13-03's InAppMetroHandle addition, that's
    expected — use the actual current line numbers.

    STEP 3 — Create src/app/mod.rs FIRST (so all other files have a home):
    ```rust
    //! App layer — TEA event loop, state mutation, effect interpretation.
    //!
    //! Split from a monolithic src/app.rs (2428 LOC) per F-200 in Plan 13-06. The
    //! submodules have single-concern roles. Plans 13-07..13-10 apply the behavior-
    //! changing refactors (F-201 TEA purity, F-202 hexagonal, F-203 metro extraction,
    //! F-204 Recipe consumer, F-205 exhaustive modal arms, F-208 KEYBINDINGS, F-209
    //! sub-struct grouping).

    pub mod adapters;
    pub mod effect;
    pub mod effect_runner;
    pub mod handle_key;
    pub mod runtime;
    pub mod state;
    pub mod update;

    // Re-exports — keeps rn_dash::app::* paths stable for tests and ui/
    pub use state::{
        active_output, active_output_scroll, active_worktree_id,
        AppState, ErrorState, FocusedPanel, PaletteMode,
    };
    pub use update::{update, dispatch_command};
    pub use runtime::run;
    pub use handle_key::handle_key;

    #[cfg(test)]
    pub mod dispatch_tests;
    ```

    NOTE: If `dispatch_command` is not a needed public export (only used internally by
    update.rs), keep it private in update.rs and drop from the re-export list. Check
    `rg 'dispatch_command' tests/ src/` — if only update.rs uses it, drop the export.
    If dispatch_tests uses it, keep the export.

    STEP 4 — Create STUBS (cheapest files first):
    src/app/adapters.rs:
    ```rust
    //! Plan 13-08 populates this file. Adapters struct holds trait objects for all
    //! infra ports (CommandRunnerPort, MetroPort, PortProbePort, WorktreePort,
    //! DevicePort, JiraPort, MultiplexerPort).
    #![allow(dead_code)]

    pub struct Adapters;
    ```

    src/app/effect_runner.rs:
    ```rust
    //! Plan 13-08 populates this file. EffectRunner consumes Vec<Effect> from
    //! update() and dispatches each effect via the Adapters struct.
    #![allow(dead_code)]

    pub struct EffectRunner;
    ```

    STEP 5 — Carve src/app/state.rs from src/app.rs:1-256. Executor uses sed/awk or
    manual copy. Move verbatim — no field additions, no renames.
    - Include: `use` statements at top of src/app.rs that state.rs actually needs
      (pare down compiler-driven)
    - Include: `pub struct AppState`, `pub enum FocusedPanel`, `pub enum ErrorState`,
      `pub enum PaletteMode`, `impl Default for AppState`, helpers `active_worktree_id`,
      `active_output`, `active_output_scroll`
    - NOTE: the `pub mod effect;` declaration Plan 13-03 added to src/app.rs top MUST NOT
      migrate to state.rs — it's a SIBLING module declaration and belongs in src/app/mod.rs
      (already added in Step 3)
    - Delete the moved content from src/app.rs (work incrementally — or keep src/app.rs
      untouched until all 6 files are created and all content accounted for, then delete)

    STEP 6 — Carve src/app/handle_key.rs from src/app.rs:258-478. Verbatim move.
    Pull needed imports (ratatui::crossterm::event, crate::domain::action::Action, etc.)
    from src/app.rs top.

    STEP 7 — Carve src/app/update.rs from src/app.rs:485-2061. This is the largest
    chunk (~1580 LOC). Verbatim move; preserve signature `pub fn update(state: &mut
    AppState, action: Action, metro_tx: ..., handle_tx: ...)` exactly.
    - Include dispatch_command (lines 485-536) — either pub or private depending on
      Step 3 decision
    - Include all `use` statements needed: Action, CommandSpec, effect (Plan 13-03),
      the domain::ports::* imports where update() already references them post-13-01,
      tokio channel types, etc.

    STEP 8 — Carve src/app/runtime.rs from src/app.rs:2063-2425. Includes:
    - pub async fn run (lines 2063-2202) — event loop
    - 7 async metro helpers (lines 2209-2425): spawn_metro_task, metro_process_task,
      parse_metro_line, extract_percent, drain_metro_output, stdin_writer, metro_http_post
    - InAppMetroHandle struct (added in Plan 13-03; typically near the metro helpers
      at file end)
    Verbatim move; all stay in runtime.rs temporarily. Plan 13-07 moves the 7 helpers +
    InAppMetroHandle to src/infra/metro.rs (replaced by TokioMetroAdapter).

    STEP 9 — `src/app/dispatch_tests.rs`:
    - If it already exists as a separate file (Phase 12 Plan 12-03 per STATE.md created
      it), VERIFY it remains in src/app/dispatch_tests.rs (same path). The module
      declaration in src/app/mod.rs already lists it.
    - If it lived inline inside src/app.rs somehow, carve it out to its own file.
    - Verify the #[cfg(test)] mod declaration in src/app/mod.rs (`pub mod dispatch_tests;`)
      makes it discoverable.

    STEP 10 — Delete src/app.rs:
    - After all content accounted for in sibling files, `rm src/app.rs` (or `git mv`
      per the workflow — git tracks rename+split heuristically).

    STEP 11 — Fix imports across the 6 new files iteratively (compiler-driven):
    - Run `cargo build --all-targets 2>&1 | head -50` — start resolving errors
      top-down
    - Most errors are: "cannot find type X in this scope" → add `use` to the file that
      needs it, OR "unused import" → prune the import
    - Update.rs needs: `use crate::app::{AppState, FocusedPanel, PaletteMode, ErrorState};`
      to access types that moved to state.rs
    - handle_key.rs needs: same imports from state.rs
    - runtime.rs needs: same imports + infra (for now — Plan 13-08 removes)

    STEP 12 — Verify all tests still pass:
    - `cargo test --lib domain::metro` — 3 tests (register_twice etc.)
    - `cargo test --lib domain::refresh` — 17 tests
    - `cargo test --lib domain::jira` — 6 tests (from 13-01)
    - `cargo test --lib domain::pipeline` — 10 tests (from 13-03)
    - `cargo test --lib domain::command` — 6+ tests (from 13-02 is_cancellable)
    - `cargo test --lib dispatch_tests` — **17 tests** (the critical COVER-03 surface —
      if this drops to 0, Pitfall 1 triggered and module declaration is missing)
    - `cargo test --test metro_single_instance` — 2 tests
    - `cargo test --test process_group_kill` — 1 test
    - `cargo clippy --all-targets -- -D warnings` exits 0
    - `make arch-lint` exits 0

    STEP 13 — Update COVERAGE-THRESHOLDS.md per Pitfall 7 procedure described above.

    STEP 14 — `make cov-check` — ensure output is valid; eyeball that per-file floors
    are still satisfied with the new row set.

    Commit: `refactor(13-06): split src/app.rs into src/app/{mod,state,update,handle_key,runtime,effect_runner,adapters}.rs per F-200; dispatch_tests + 17 COVER-03 tests preserved`.
  </action>
  <verify>
    <automated>cargo build --all-targets && cargo test --all-targets --quiet 2>&1 | tail -10 | grep -q 'test result: ok' && cargo test --lib dispatch_tests --quiet 2>&1 | tail -3 | grep -q '17 passed' && cargo clippy --all-targets -- -D warnings && test ! -f src/app.rs && test -f src/app/mod.rs && test -f src/app/state.rs && test -f src/app/update.rs && test -f src/app/handle_key.rs && test -f src/app/runtime.rs && test -f src/app/effect_runner.rs && test -f src/app/adapters.rs && grep -q '#\[cfg(test)\]' src/app/mod.rs && grep -q 'pub mod dispatch_tests' src/app/mod.rs && make arch-lint</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `src/app.rs` DELETED (`test ! -f src/app.rs` exits 0)
    - [ ] 6 new submodule files exist: mod.rs, state.rs, update.rs, handle_key.rs, runtime.rs, effect_runner.rs, adapters.rs (7 total — mod + 6 others)
    - [ ] src/app/mod.rs contains `#[cfg(test)] pub mod dispatch_tests;` declaration (Pitfall 1 avoided)
    - [ ] `cargo test --lib dispatch_tests` output shows **17 passed** — the critical COVER-03 surface survives the split
    - [ ] `cargo build --all-targets` exits 0
    - [ ] `cargo test --all-targets` exits 0 — 46+ lib tests (including all domain + dispatch_tests) + 3 integration tests
    - [ ] `cargo clippy --all-targets -- -D warnings` exits 0
    - [ ] `cargo test --test metro_single_instance` passes 2 tests
    - [ ] `cargo test --test process_group_kill` passes 1 test
    - [ ] `make arch-lint` exits 0
    - [ ] `rn_dash::app::update`, `rn_dash::app::run`, `rn_dash::app::handle_key`, `rn_dash::app::AppState` — all resolve (test files import from these paths)
    - [ ] .planning/phases/12-coverage-gate/COVERAGE-THRESHOLDS.md updated with new per-file rows + changelog entry
    - [ ] `make cov-check` runs without error (may show different per-file breakdown; human-verify it's consistent)
    - [ ] InAppMetroHandle bridge from Plan 13-03 lives in src/app/runtime.rs now (Plan 13-07 moves it to infra/metro.rs)
  </acceptance_criteria>
  <done>
    src/app.rs split into 7 files under src/app/; 17 dispatch tests preserved; 49 existing tests all pass; clippy clean; coverage thresholds file updated per structural-change policy. No behavior change — every refactor from this plan is lift-and-shift.
  </done>
</task>

</tasks>

<verification>
- `cargo test --lib dispatch_tests` shows **17 passed** (COVER-03 surface preserved — Pitfall 1 avoided)
- `cargo test --all-targets` exits 0
- `cargo clippy --all-targets -- -D warnings` exits 0
- `test ! -f src/app.rs && test -f src/app/mod.rs` exits 0
- `make arch-lint` exits 0
- `make cov-check` shows a valid per-file breakdown (new rows for the 7 files)
- No new Critical findings introduced; all existing CoverS-01, COVER-02, COVER-03 assertions still hold
</verification>

<success_criteria>
F-200 structural split complete. src/app/ directory with 7 files. All 49 existing tests pass. COVERAGE-THRESHOLDS.md updated. Plan 13-07 can now rewrite update() to be pure (F-201), move metro helpers to infra (F-203), and implement KEYBINDINGS-driven handle_key (F-208) because each concern has its own file.
</success_criteria>

<output>
After completion, create `.planning/phases/13-audit-driven-refactors/13-06-SUMMARY.md` documenting:
- Final line counts per file (mod.rs, state.rs, update.rs, handle_key.rs, runtime.rs, effect_runner.rs, adapters.rs)
- Total line count before (2428) vs after (sum of the 7 files — should be close; small difference from removing duplicate `use` statements)
- `cargo test --lib dispatch_tests` output (expect "17 passed")
- COVERAGE-THRESHOLDS.md diff excerpt
- Any test that unexpectedly broke and required a fix (ideally none — structural lift only)
- Whether `dispatch_command` ended up pub or private
</output>
