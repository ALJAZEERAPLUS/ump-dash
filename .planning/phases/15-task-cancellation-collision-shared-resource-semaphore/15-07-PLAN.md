---
phase: 15-task-cancellation-collision-shared-resource-semaphore
plan: 07
type: execute
wave: 5
depends_on: ["15-05"]
files_modified:
  - .planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-VALIDATION.md
autonomous: true
requirements:
  - TASK-04
  - TASK-05
  - TASK-06
tags: [validation, documentation, nyquist, test-map]
must_haves:
  truths:
    - "15-VALIDATION.md has nyquist_compliant: true and wave_0_complete: true set in its frontmatter"
    - "The Per-Task Verification Map table is populated with one row per task across all 6 implementation plans (15-01..15-06), each row carrying Plan ID, Wave, Requirement (TASK-04/05/06), Threat Ref (T-15-XX), Test Type, and Automated Command"
    - "The Test Infrastructure block names framework=cargo test, quick run=cargo test --lib --quiet, full suite=cargo test --workspace, estimated runtime <30 seconds"
    - "Wave 0 Requirements section lists the two new integration test files from Plan 15-06 as the only Wave 0 dependencies (now satisfied)"
    - "Manual-Only Verifications section explicitly says: All phase behaviors have automated verification — no manual UAT items required for Phase 15"
    - "Validation Sign-Off checklist has all boxes ticked + Approval: approved YYYY-MM-DD with the actual completion date"
  artifacts:
    - path: .planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-VALIDATION.md
      provides: "Filled-in Nyquist-compliant validation contract for Phase 15"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "15-VALIDATION.md Per-Task Verification Map"
      to: "Each plan's <task><verify><automated> command"
      via: "One-to-one row mapping"
      pattern: "cargo test"
---

<objective>
Fill in the empty `15-VALIDATION.md` template that 15-RESEARCH placed in
the phase directory. The template currently has placeholder `{XX}` markers,
`nyquist_compliant: false`, and `wave_0_complete: false`. After this plan,
the file declares a comprehensive test map covering every implementation
task in Plans 15-01..15-06, names each automated command, ties each task
to its requirement (TASK-04/05/06) and its threat-model reference
(T-15-XX from each plan's `<threat_model>` block), and flips the
frontmatter flags to true.

Purpose: 15-VALIDATION.md is the per-phase validation contract — the source
of truth for feedback sampling during execution and for the `/gsd:verify-work`
gate. Without filling it in, the verify step has no map of "which test
covers which requirement" and falls back to global `cargo test --workspace`
which doesn't tell us anything about coverage gaps.

Output: 1 file rewritten end-to-end (~120 LOC of markdown table + frontmatter
flips). No code changes. No tests run as part of this plan beyond the verify
command — but the verify command IS validating that every test named in the
map actually exists and passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-VALIDATION.md
@.planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-RESEARCH.md
@.planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-01-PLAN.md
@.planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-02-PLAN.md
@.planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-03-PLAN.md
@.planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-04-PLAN.md
@.planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-05-PLAN.md
@.planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-06-PLAN.md

<interfaces>
<!-- Map of each plan's tasks → their <verify><automated> commands. This is the source data for the Per-Task Verification Map table. -->

Plan 15-01 (Wave 1) — three tasks:
- 15-01-01: tokio-util direct dep promotion → `cargo metadata ... | jq ... tokio-util && cargo build --quiet` (T-15-01-SC) — TASK-04
- 15-01-02: CommandEvent::ProcessStarted variant → `cargo test --lib command_runner_port::tests::process_started_variant_constructs` (T-15-01-01) — TASK-04
- 15-01-03: .process_group(0) + ProcessStarted emission → `cargo test --lib command_runner::tests::run_command_emits_process_started_first && cargo test --test process_group_kill` (T-15-01-01) — TASK-04

Plan 15-02 (Wave 2) — three tasks:
- 15-02-01: 3-field TokioTaskHandle → `cargo test --lib task_handle::tests::construct_with_all_three_fields task_handle::tests::boxed_task_handle_dispatches_through_trait_object` (T-15-02-01) — TASK-04
- 15-02-02: SIGTERM→200ms→SIGKILL abort() → `cargo test --lib task_handle::tests::abort_with_placeholder_pid_zero_is_noop ::abort_with_placeholder_pid_one_is_noop ::abort_with_dead_pid_does_not_panic` (T-15-02-02) — TASK-04
- 15-02-03: signal-aware From<ExitStatus> → `cargo test --lib task_handle::tests::from_sigkill_status_maps_to_killed ::from_sigterm_status_maps_to_cancelled` (T-15-02-03) — TASK-04

Plan 15-04 (Wave 2) — one task:
- 15-04-01: CollisionPolicy + collision_policy() → `cargo test --lib command::tests::collision_policy_idempotent_installs_block_new ::collision_policy_builds_tests_runs_cancel_previous ::collision_policy_git_variants_all_block_new ::collision_policy_covers_every_variant` (T-15-04-01) — TASK-05

Plan 15-03 (Wave 3) — two tasks:
- 15-03-01: Effect::SpawnTask + repo_root field → `cargo test --lib effect::tests::spawn_task_carries_repo_root_for_semaphore_key effect::tests::effect_variants_compile effect::tests::effect_has_at_least_fifteen_variants` (T-15-03-02) — TASK-06
- 15-03-02: SpawnTask arm extension → `cargo build --quiet && cargo test --lib --quiet` (T-15-03-01, T-15-03-03) — TASK-04 + TASK-06

Plan 15-05 (Wave 4) — three tasks:
- 15-05-01: CommandCancel is_cancellable() guard → `cargo build --quiet && cargo test --lib --quiet` (T-15-05-01) — TASK-04
- 15-05-02: dispatch_command collision gate → `cargo build --quiet && cargo test --lib --quiet` (T-15-05-03) — TASK-05
- 15-05-03: 6 inline dispatch_tests in mod collision + mod cancellation_guard → `cargo test --lib dispatch_tests::collision dispatch_tests::cancellation_guard` (T-15-05-01, T-15-05-03) — TASK-04 + TASK-05

Plan 15-06 (Wave 5) — two tasks:
- 15-06-01: tests/process_group_cancel.rs → `cargo test --test process_group_cancel --quiet` (T-15-06-01, T-15-06-02) — TASK-04
- 15-06-02: tests/yarn_semaphore_serializes.rs → `cargo test --test yarn_semaphore_serializes --quiet` (T-15-06-01) — TASK-06

Test framework (rn-dash crate is bin+lib per Phase 12 Plan 12-00 — tests/*.rs can import rn_dash::*):
- Framework: cargo test built-in + #[tokio::test]
- Quick run: cargo test --lib --quiet (about 5-10 seconds for ~100 tests)
- Full suite: cargo test --workspace (about 15-20 seconds incl. integration tests)
- Max feedback latency: ~20 seconds (well under the 15-second Nyquist target for unit tests; integration tests up to 30s combined)

Wave 0 status:
- 15-RESEARCH §Wave 0 Gaps listed:
  - tests/process_group_cancel.rs — covered by Plan 15-06
  - tests/yarn_semaphore_serializes.rs — covered by Plan 15-06
  - Inline test fn collision_block_new — covered by Plan 15-05 Task 3
  - Inline test fn collision_cancel_previous — covered by Plan 15-05 Task 3
  - Inline test fn is_cancellable_gate_in_cancel_handler — covered by Plan 15-05 Task 3
  - Unit tests for collision_policy() — covered by Plan 15-04 Task 1
- All Wave 0 gaps closed before /gsd:verify-work.

Manual-only verifications:
- Phase 15 produces zero new UI behaviors (Phase 16 owns spinner / elapsed). All cancellation, collision, and serialization behaviors are observable via test assertions on state mutations + subprocess probe assertions.
- Therefore: "All phase behaviors have automated verification." Per the 15-VALIDATION template, the table is reduced to a single sentence rather than empty rows.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite 15-VALIDATION.md end-to-end — populate frontmatter, test map, sign-off</name>
    <files>.planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-VALIDATION.md</files>
  <read_first>
    - .planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-VALIDATION.md (the template — entire 77-line file)
    - .planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-RESEARCH.md §Validation Architecture (the canonical test map)
    - Each of 15-01-PLAN.md through 15-06-PLAN.md (the actual <verify> commands — already extracted in the <interfaces> section above; cross-check against the plan files to ensure no command typos)
    - .planning/phases/14-per-worktree-task-system-foundation/14-VALIDATION.md (the Phase 14 exemplar — confirm the table column order, the frontmatter style, the sign-off section)
  </read_first>
  <behavior>
    - Behavior 1: The frontmatter at the top of `15-VALIDATION.md` is updated:
      - `status: draft` → `status: approved`
      - `nyquist_compliant: false` → `nyquist_compliant: true`
      - `wave_0_complete: false` → `wave_0_complete: true`
      - `created: 2026-05-18` (preserve)
      - Add `approved: YYYY-MM-DD` with today's date.
    - Behavior 2: The `## Test Infrastructure` table is filled in:
      - Framework: `cargo test (built-in) + #[tokio::test] via tokio "macros"`
      - Config file: `Cargo.toml [dev-dependencies]; no separate config`
      - Quick run command: `cargo test --lib --quiet`
      - Full suite command: `cargo test --workspace`
      - Estimated runtime: `~20 seconds (lib: ~8s; integration: ~10s additional)`
    - Behavior 3: The `## Sampling Rate` block is filled in:
      - After every task commit: `cargo test --lib --quiet` (about 8 seconds; well under the 15s Nyquist budget)
      - After every plan wave: `cargo test --workspace && make arch-lint`
      - Before `/gsd:verify-work`: full suite green + clippy clean + arch-lint green
      - Max feedback latency: ~20 seconds wall-clock
    - Behavior 4: The `## Per-Task Verification Map` table is rewritten with one row per the 14 tasks listed in the <interfaces> data. Columns per the template: Task ID, Plan, Wave, Requirement, Threat Ref, Secure Behavior, Test Type, Automated Command, File Exists, Status. For "File Exists", use ✅ for tests that exist after that plan completes and ❌ W0 for files that don't exist yet (the Wave 0 entries from Plan 15-06 should be ✅ by the time this plan runs, since 15-06 is in Wave 5 and this plan is also in Wave 5; check actual file existence at execution time). For "Status", initial value is ⬜ pending — this gets flipped to ✅ green during /gsd:verify-work.
    - Behavior 5: The `## Wave 0 Requirements` section is rewritten:
      - All entries that were Wave 0 dependencies are now ✅ satisfied by Plan 15-06.
      - The list explicitly names: `tests/process_group_cancel.rs` (Plan 15-06 Task 1), `tests/yarn_semaphore_serializes.rs` (Plan 15-06 Task 2), and all 6 inline tests + 4 collision_policy tests covered by Plans 15-04 / 15-05.
      - Sentence: "All Wave 0 dependencies satisfied by the time Plan 15-07 runs (Plan 15-06 in the same wave)."
    - Behavior 6: The `## Manual-Only Verifications` section is rewritten to: "All phase behaviors have automated verification." (Single sentence; table omitted per template guidance.)
    - Behavior 7: The `## Validation Sign-Off` checklist has all 6 boxes ticked:
      - [x] All tasks have `<automated>` verify or Wave 0 dependencies — every row has a command, none missing
      - [x] Sampling continuity: no 3 consecutive tasks without automated verify — confirmed by scanning the Per-Task Verification Map
      - [x] Wave 0 covers all MISSING references — Plan 15-06 produces both test files
      - [x] No watch-mode flags — `--quiet`, `--workspace`, `--lib` only; no `--watch`
      - [x] Feedback latency < 15s for quick run, < 25s for full suite — confirmed by Behavior 2 timings
      - [x] `nyquist_compliant: true` set in frontmatter — confirmed by Behavior 1
      - Approval: `approved 2026-05-XX` (today's date in YYYY-MM-DD).
  </behavior>
  <action>
    Edit `.planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-VALIDATION.md`:
    1. Update the frontmatter per Behavior 1.
    2. Replace `## Test Infrastructure` table content per Behavior 2.
    3. Replace `## Sampling Rate` bullet content per Behavior 3.
    4. Replace `## Per-Task Verification Map` table — delete the single placeholder row and add 14 rows, one per task from the <interfaces> data. Use markdown table syntax with the columns in the template header order. Truncate long `Automated Command` cells if needed but ensure each command is unambiguous and copy-pasteable.
    5. Replace `## Wave 0 Requirements` content per Behavior 5.
    6. Replace `## Manual-Only Verifications` table with the single-sentence form per Behavior 6.
    7. Replace `## Validation Sign-Off` content per Behavior 7. Use today's actual date as YYYY-MM-DD (the executor at run-time should substitute `$(date +%Y-%m-%d)` or read the date from system metadata — when generating this VALIDATION.md, the planner does not know the future execution date; the executor running this plan will fill in the actual date).

    Do NOT modify any other file. Do NOT add new sections to 15-VALIDATION.md. Do NOT create a new VALIDATION-NN.md.
  </action>
  <verify>
    <automated>grep -q 'nyquist_compliant: true' .planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-VALIDATION.md &amp;&amp; grep -q 'wave_0_complete: true' .planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-VALIDATION.md &amp;&amp; grep -q '| 15-01-01 |' .planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-VALIDATION.md &amp;&amp; grep -q '| 15-06-02 |' .planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-VALIDATION.md &amp;&amp; grep -c '| 15-' .planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-VALIDATION.md | awk '{if ($1 < 14) { print "ERROR: expected at least 14 task rows, got " $1; exit 1 } else print "OK: " $1 " task rows" }'</automated>
  </verify>
  <acceptance_criteria>
    - `15-VALIDATION.md` frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`.
    - The Per-Task Verification Map has at least 14 task rows (one per task in plans 15-01..15-06: 3+3+2+1+3+2 = 14).
    - Every row's Automated Command column contains a valid `cargo test ...` or `cargo build ...` or `grep -q ...` invocation that is also present in the corresponding PLAN.md's `<verify><automated>` block.
    - The Wave 0 Requirements section lists `tests/process_group_cancel.rs` and `tests/yarn_semaphore_serializes.rs` as previously-MISSING items, now ✅ closed.
    - The Manual-Only Verifications section has the single sentence "All phase behaviors have automated verification."
    - The Sign-Off section has all 6 boxes ticked and a dated `approved` line.
    - The file is valid markdown (frontmatter parses; no broken table syntax).
  </acceptance_criteria>
  <done>15-VALIDATION.md is Nyquist-compliant; every task in plans 15-01..15-06 has a row with an automated command + threat-ref + requirement; Wave 0 is fully closed; manual verifications are explicitly N/A.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner-written test map → /gsd:verify-work gate | The map is the contract that verify-work consults to know which tests must pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07-01 | Repudiation | Test map omits a task → /gsd:verify-work passes despite a real coverage gap | mitigate | Acceptance criteria require ≥14 rows (the exact total of tasks across plans 15-01..15-06); the verify command's `grep -c '| 15-'` count enforces the minimum. |
| T-15-07-02 | Tampering | Automated Command typo causes /gsd:verify-work to silently skip a test (e.g., misspelled test name returns "0 tests run") | mitigate | Acceptance criteria require each command to be present in the corresponding PLAN.md `<verify><automated>` block — manual cross-check during execution + greppable. Mitigation reinforced by `/gsd:verify-work` running the commands and treating "0 tests filtered" as a failure (per gsd-sdk convention). |
| T-15-07-SC | — | No packages | — | N/A — pure markdown edit. |
</threat_model>

<verification>
After Task 1:
- 15-VALIDATION.md frontmatter flags both `true`
- 14+ task rows in the Per-Task Verification Map
- All commands are valid cargo invocations
- File parses as valid markdown (no broken tables)
- `/gsd:verify-work` will read this map and run every command listed
</verification>

<success_criteria>
- The empty 15-VALIDATION.md template is now a complete validation contract.
- Every task across the 6 implementation plans has a documented automated verify command, threat reference, and requirement tag.
- Wave 0 is officially closed (Plan 15-06 produces the two integration tests that 15-RESEARCH §Wave 0 Gaps flagged).
- /gsd:verify-work has an actionable map for the Phase 15 verification gate.
- No manual UAT items required for Phase 15 — automated verification is complete.
</success_criteria>

<output>
Create `.planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-07-SUMMARY.md` when done.
</output>
