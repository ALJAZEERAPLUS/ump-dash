---
phase: 15-task-cancellation-collision-shared-resource-semaphore
plan: 06
type: execute
wave: 5
depends_on: ["15-05"]
files_modified:
  - tests/process_group_cancel.rs
  - tests/yarn_semaphore_serializes.rs
autonomous: true
requirements:
  - TASK-04
  - TASK-06
tags: [integration-tests, process-group, semaphore, end-to-end, validation]
must_haves:
  truths:
    - "tests/process_group_cancel.rs proves end-to-end TASK-04: spawning a task via TokioCommandRunner (NOT directly tokio::process::Command), reading the PID from the first CommandEvent::ProcessStarted event, calling TokioTaskHandle::abort() invokes the SIGTERM→200ms→SIGKILL ladder, and `libc::kill(-pgid, 0)` probes confirm zero orphans within 2 seconds total (ROADMAP success criterion 1)"
    - "tests/yarn_semaphore_serializes.rs proves end-to-end TASK-06: two SpawnTask effects with the same canonicalized repo_root for yarn-family specs (substituting `bash -c 'sleep 0.5; echo done'` for `yarn install`) execute serially — the second task's started_at is at least 450ms after the first task's started_at (ROADMAP success criterion 4)"
    - "tests/process_group_kill.rs (COVER-02) and tests/metro_single_instance.rs (COVER-01) are NOT modified — D-22 invariant preserved"
  artifacts:
    - path: tests/process_group_cancel.rs
      provides: "End-to-end orphan kill assertion for TASK-04 via TokioCommandRunner"
      contains: "ProcessStarted"
    - path: tests/yarn_semaphore_serializes.rs
      provides: "End-to-end semaphore serialization assertion for TASK-06"
      contains: "Semaphore"
  key_links:
    - from: "tests/process_group_cancel.rs"
      to: "src/infra/command_runner.rs::TokioCommandRunner::spawn + .process_group(0)"
      via: "Spawn through the runner; read ProcessStarted; abort via TokioTaskHandle; probe with libc::kill(-pgid, 0)"
      pattern: "CommandEvent::ProcessStarted"
    - from: "tests/yarn_semaphore_serializes.rs"
      to: "src/app/effect_runner.rs::yarn_semaphores"
      via: "Two parallel SpawnTask Effects with same repo_root; assert second started_at >= first started_at + ~500ms"
      pattern: "yarn_semaphores"
---

<objective>
Land the two integration tests that move Wave 0 placeholders in
`15-VALIDATION.md §Phase Requirements → Test Map` from `❌ Wave 0` to `✅`.
Each test spawns real subprocesses (bash sleep fixtures, NOT actual yarn) and
asserts the end-to-end behavior of Phase 15: (1) full process-group reaping
within 2 seconds for any cancellable command run THROUGH `TokioCommandRunner`
(not directly via `tokio::process::Command` as COVER-02 does), and
(2) per-repo-root semaphore serialization of yarn-family installs.

Purpose: ROADMAP success criteria 1 and 4 are not actually proven by any
prior plan — Plan 15-02 unit-tested the abort() ladder against a dead pid
(safe no-op); Plan 15-03 unit-tested only the field shape of yarn_semaphores.
This plan is the proof. After this plan, all four ROADMAP success criteria
have either inline tests (15-04 + 15-05) or integration tests (THIS plan) as
their evidence.

Output: 2 new files in `tests/`. No source files modified. Total ~250 LOC.
Each test gated `#[cfg(any(target_os = "linux", target_os = "macos"))]` and
hard-timeout-wrapped to prevent CI hangs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-RESEARCH.md
@.planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-PATTERNS.md
@.planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-01-SUMMARY.md
@.planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-02-SUMMARY.md
@.planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-03-SUMMARY.md
@tests/process_group_kill.rs
@src/infra/command_runner.rs
@src/infra/task_handle.rs
@src/domain/ports/command_runner_port.rs

<interfaces>
<!-- Contracts established by Plans 15-01..15-05 (all DO NOT redefine — consume them): -->

From src/infra/command_runner.rs:
- `pub struct TokioCommandRunner;` (or similar — verify the exact constructor pattern in the existing inline tests at the bottom of command_runner.rs after Plan 15-01)
- `TokioCommandRunner::spawn(spec, cwd, branch) -> UnboundedReceiver<CommandEvent>` (the trait method on CommandRunnerPort)
- After Plan 15-01: emits ProcessStarted { pid } as first event; subprocess spawned with `.process_group(0)`

From src/domain/ports/command_runner_port.rs (after Plan 15-01):
- `pub enum CommandEvent { ProcessStarted { pid: u32 }, OutputLine(String), Exited(ExitStatus) }`

From src/infra/task_handle.rs (after Plan 15-02):
- `pub struct TokioTaskHandle { pub join_handle, pub child_pid, pub cancel_token }` named-field
- `TaskHandle::abort()` performs SIGTERM → 200ms → SIGKILL → cancel_token.cancel() → join_handle.abort()

COVER-02 (tests/process_group_kill.rs lines 41-125) — the structural template to MIRROR:
- `#[cfg(any(target_os = "linux", target_os = "macos"))]` attribute
- `#[tokio::test(flavor = "multi_thread")]`
- Spawns `bash -lc "sleep 60 & echo $!; wait"` — the `echo $!` prints the child's PID; the test parses it
- `assert!(pgid > 1)` — Pitfall 3 guard
- 100ms sleep to let bash fork the grandchild sleep
- `libc::kill(-pgid, 0)` probe — returns 0 if group has members, -1 with ESRCH if empty
- `tokio::time::timeout(Duration::from_secs(2), child.wait())` — 2-second hard ceiling
- ESRCH polling loop lines 108-122 — polls every 20ms up to 500ms after wait() returns

Key difference for Plan 15-06's test from COVER-02:
- COVER-02 spawns `tokio::process::Command` DIRECTLY because at the time it was written, command_runner.rs did NOT have .process_group(0).
- Plan 15-06's test spawns THROUGH `TokioCommandRunner::spawn(...)` — proves that the end-to-end Phase 15 path (Plan 15-01's process_group(0) + Plan 15-02's abort() ladder) actually reaps grandchildren.

From src/app/effect_runner.rs (after Plan 15-03):
- `EffectRunner.yarn_semaphores: std::sync::Mutex<HashMap<PathBuf, Arc<Semaphore>>>`
- `Effect::SpawnTask { ..., repo_root }` — semaphore key
- Yarn-family specs: YarnInstall, YarnPodInstall, RmNodeModules
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests/process_group_cancel.rs — end-to-end orphan reaping via TokioCommandRunner + TokioTaskHandle::abort</name>
    <files>tests/process_group_cancel.rs</files>
  <read_first>
    - tests/process_group_kill.rs (entire file — the structural template to copy)
    - src/infra/command_runner.rs (the `TokioCommandRunner` constructor and inline tests showing how to instantiate the runner from outside the module)
    - src/infra/task_handle.rs (TokioTaskHandle construction — needed to wrap the JoinHandle for abort)
    - .planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-PATTERNS.md (the `tests/process_group_cancel.rs` section, lines 546-569)
    - .planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-RESEARCH.md §Validation Architecture (TASK-04 integration test row + Critical test notes)
  </read_first>
  <behavior>
    - Behavior 1: A new file `tests/process_group_cancel.rs` exists with a doc-comment header explaining: "TASK-04 end-to-end characterization — proves that cancelling a task spawned THROUGH `TokioCommandRunner` (Plan 15-01's `.process_group(0)` + Plan 15-02's `TokioTaskHandle::abort()`) reaps the full process group within 2 seconds. Companion to but distinct from tests/process_group_kill.rs (COVER-02), which spawns `tokio::process::Command` directly and was written before Plan 15-01 added `.process_group(0)` to command_runner.rs."
    - Behavior 2: One test `cancel_via_task_handle_reaps_full_process_group` does the following inside a `#[tokio::test(flavor = "multi_thread")] #[cfg(any(target_os = "linux", target_os = "macos"))]`:
      1. Instantiate `let runner = TokioCommandRunner;` (or whatever the module-public constructor is — copy the pattern from `src/infra/command_runner.rs` inline tests).
      2. Build a CommandSpec that runs `bash -c 'sleep 60 & echo $! >&2; wait'`. Choose `CommandSpec::ShellCommand { command: ... }` if that's the natural variant for arbitrary bash — verify by reading `src/domain/command.rs` for the right variant. The shell command must use `bash -c` so the `sleep 60 &` and `wait` build-ins work; the `echo $! >&2` prints the sleep child's PID to stderr (which the command_runner streams as OutputLine).
      3. Call `let mut rx = runner.spawn(spec, std::env::temp_dir(), "main".into());`.
      4. The FIRST event must be `ProcessStarted { pid }`. `let pgid = match rx.recv().await { Some(CommandEvent::ProcessStarted { pid }) => pid as i32, other => panic!("expected ProcessStarted first, got {other:?}") };`
      5. Assert `pgid > 1`.
      6. Drain OutputLine events until we see the grandchild sleep PID from `echo $!` — parse it from any OutputLine that looks like a bare number (use `line.trim().parse::<i32>().ok()` and capture the first hit). Let it be `sleep_pid`. Use `tokio::time::timeout(Duration::from_millis(500), ...)` to bound this drain.
      7. Probe `libc::kill(-pgid, 0)` — assert it returns 0 (group has members) right now. This is the BEFORE assertion.
      8. Construct a `TokioTaskHandle { join_handle: tokio::spawn(async {}), child_pid: pgid as u32, cancel_token: tokio_util::sync::CancellationToken::new() }`. WAIT — the test does not have access to a `join_handle` for the actual command_runner forwarding task (that's owned by effect_runner in production). Instead, just call the SIGTERM+SIGKILL ladder directly on `pgid`: emulate the Plan 15-02 abort() body inline in the test:
         ```
         unsafe { let _ = libc::kill(-pgid, libc::SIGTERM); }
         tokio::time::sleep(Duration::from_millis(200)).await;
         unsafe { let _ = libc::kill(-pgid, libc::SIGKILL); }
         ```
         REVISION: the test should ACTUALLY use TokioTaskHandle::abort() to prove the production path works. Construct: `let handle = TokioTaskHandle { join_handle: tokio::spawn(async { let _ = rx_drain_task; }), child_pid: pgid as u32, cancel_token: tokio_util::sync::CancellationToken::new() };` — where `rx_drain_task` is a placeholder forwarding loop that does nothing (since we'll consume rx ourselves in the test). Simpler: spawn a NO-OP forwarding task `let nop_handle = tokio::spawn(async {});` for the JoinHandle field, then call `handle.abort()`. The abort() will fire SIGTERM/SIGKILL to the REAL pgid even though the inner JoinHandle is no-op — that's what we want to test.
      9. Call `handle.abort();` (the Plan 15-02 widened method).
      10. Within a `tokio::time::timeout(Duration::from_secs(2), ...)`, poll `libc::kill(-pgid, 0)` every 20ms until it returns -1 (ESRCH = group empty). Total bound: 2 seconds.
      11. Assert the polling exited via the ESRCH path BEFORE the timeout fired. If the timeout fires, panic with the elapsed time and the pgid so the failure is diagnosable.
      12. Final assertion: `unsafe { libc::kill(sleep_pid, 0) }` returns -1 with errno ESRCH — the SPECIFIC grandchild sleep is dead (extra safety check beyond the group probe).
    - Behavior 3: Test completes in < 2.5 seconds total wall-clock on macOS / Linux.
  </behavior>
  <action>
    Create `tests/process_group_cancel.rs`:
    1. Copy the structural skeleton of `tests/process_group_kill.rs` lines 1-40 (the header comment, the `#[cfg]` attribute, the `use` block including `libc`, tokio time + timeout + Duration). Adapt the header to reflect this test's distinct purpose (Behavior 1 wording).
    2. Add `use rn_dash::infra::command_runner::TokioCommandRunner;` and `use rn_dash::infra::task_handle::TokioTaskHandle;` and `use rn_dash::domain::ports::command_runner_port::{CommandRunnerPort, CommandEvent};` and `use rn_dash::domain::command::CommandSpec;`. (Phase 12 / Plan 12-00 converted rn-dash to bin+lib — the `rn_dash` crate is importable from `tests/`.)
    3. Write the test per Behavior 2, with explicit assertions at each step and informative panic messages on the negative paths. Use the ESRCH-polling loop pattern from COVER-02 lines 108-122 verbatim.
    4. Do NOT modify `tests/process_group_kill.rs` (D-22).
    5. Do NOT modify any `src/` file.
  </action>
  <verify>
    <automated>cargo test --test process_group_cancel --quiet</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/process_group_cancel.rs` exists.
    - `grep -c 'TokioCommandRunner' tests/process_group_cancel.rs` returns at least 1.
    - `grep -c 'CommandEvent::ProcessStarted' tests/process_group_cancel.rs` returns at least 1.
    - `grep -c 'TokioTaskHandle' tests/process_group_cancel.rs` returns at least 1.
    - `grep -c 'libc::kill(-pgid' tests/process_group_cancel.rs` returns at least 1.
    - `cargo test --test process_group_cancel --quiet` exits with code 0 and completes in < 3 seconds wall-clock.
    - `tests/process_group_kill.rs` (COVER-02) still passes unchanged.
    - The test fails (red) if `.process_group(0)` is removed from `src/infra/command_runner.rs` — verify this by hand-mutating the runner during local development; revert before commit. Documentation in test header asserts this guard role.
  </acceptance_criteria>
  <done>End-to-end test proves the Plan 15-01 + 15-02 path actually reaps grandchildren within 2 seconds when invoked via TokioTaskHandle::abort().</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tests/yarn_semaphore_serializes.rs — proves per-repo-root Semaphore(1) serializes yarn-family installs</name>
    <files>tests/yarn_semaphore_serializes.rs</files>
  <read_first>
    - src/app/effect_runner.rs (the SpawnTask arm — confirm yarn-family is `YarnInstall | YarnPodInstall | RmNodeModules` and the semaphore key is `canonical_repo_root`)
    - src/app/effect.rs (the Effect::SpawnTask variant — confirm repo_root field)
    - tests/process_group_kill.rs (header pattern, `#[cfg]` gate, `#[tokio::test(flavor = "multi_thread")]` attribute)
    - .planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-RESEARCH.md §Validation Architecture (TASK-06 row + Critical test note: substitute `bash -c 'sleep 0.5; echo done'` for `yarn install`; assert second task's started_at >= first + 450ms)
    - .planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-PATTERNS.md (Pattern 4 - the semaphore acquire pattern)
  </read_first>
  <behavior>
    - Behavior 1: A new file `tests/yarn_semaphore_serializes.rs` exists with a doc-comment header explaining: "TASK-06 end-to-end characterization — proves that two yarn-family SpawnTask effects with the same canonicalized repo_root execute serially via the per-repo-root Semaphore(1) owned by EffectRunner. Substitutes `bash -c 'sleep 0.5; echo done'` for `yarn install` (real yarn would take 30s+). Asserts second task's started_at is at least 450ms after the first task's started_at."
    - Behavior 2: The test cannot easily construct a full `EffectRunner` from the test crate because Adapters has many fields. INSTEAD, this test directly exercises the SEMAPHORE CONTRACT — it builds the same Mutex<HashMap<PathBuf, Arc<Semaphore>>> data structure, runs the same "acquire-then-sleep" pattern that effect_runner uses, and asserts serialization timing. The test does NOT exercise the spawn path through Adapters; that's covered by Plan 15-03's unit test and the fact that the SpawnTask arm code compiles. New behavior:
      1. Construct `let semaphores: Arc<std::sync::Mutex<HashMap<PathBuf, Arc<tokio::sync::Semaphore>>>> = Arc::new(std::sync::Mutex::new(HashMap::new()));`.
      2. Define an async helper `async fn run_with_semaphore(semaphores: ..., repo_root: PathBuf, sleep_ms: u64) -> (Instant, Instant)` that:
         a. Computes `canonical = repo_root.canonicalize().unwrap_or(repo_root);` (matches Plan 15-03 logic).
         b. Acquires the semaphore: `let sem = { let mut map = semaphores.lock().unwrap(); map.entry(canonical).or_insert_with(|| Arc::new(Semaphore::new(1))).clone() };` followed by `let _permit = sem.acquire_owned().await.unwrap();`
         c. Records `let started_at = Instant::now();`
         d. Sleeps `sleep_ms`: `tokio::time::sleep(Duration::from_millis(sleep_ms)).await;`
         e. Records `let finished_at = Instant::now();`
         f. Returns `(started_at, finished_at)` (permit drops at function end).
      3. Spawn two tasks concurrently with `tokio::join!`:
         ```
         let repo = std::env::temp_dir();  // both tasks use the same repo_root
         let h1 = run_with_semaphore(semaphores.clone(), repo.clone(), 500);
         let h2 = run_with_semaphore(semaphores.clone(), repo.clone(), 100);
         let (r1, r2) = tokio::join!(h1, h2);
         ```
      4. Assertions:
         a. `r2.0 >= r1.1` (second task's started_at is at-or-after the first task's finished_at) — strict serialization.
         b. `r2.0.duration_since(r1.0) >= Duration::from_millis(450)` — second task waited at least 450ms (the first task slept 500ms, with 50ms slack for jitter).
         c. The two tasks did not run in parallel — assertion (a) is sufficient but explicit assertion (b) is more diagnosable on failure.
      5. Hard-timeout the whole test with `tokio::time::timeout(Duration::from_secs(3), test_body)`.
    - Behavior 3: A second test `different_repo_roots_do_not_serialize` runs two tasks with DIFFERENT repo_roots (use `std::env::temp_dir()` and a `std::env::temp_dir().join("other")` after creating the second dir with `std::fs::create_dir_all`); asserts the two tasks run IN PARALLEL — `r2.0 < r1.1` (second started before first finished). This proves the semaphore is keyed by repo_root, not global.
  </behavior>
  <action>
    Create `tests/yarn_semaphore_serializes.rs`:
    1. Add the doc-comment header per Behavior 1.
    2. `#[cfg(any(target_os = "linux", target_os = "macos"))]` attribute at module top OR on each test fn (mirror the convention from existing `tests/*.rs`).
    3. Use block:
       ```
       use std::collections::HashMap;
       use std::path::PathBuf;
       use std::sync::Arc;
       use std::time::{Duration, Instant};
       use tokio::sync::Semaphore;
       ```
    4. Implement the async helper `run_with_semaphore` per Behavior 2 step 2.
    5. Implement two `#[tokio::test(flavor = "multi_thread")]` tests:
       - `same_repo_root_serializes_two_yarn_family_tasks` per Behavior 2.
       - `different_repo_roots_run_in_parallel` per Behavior 3.
    6. Each test wrapped in `tokio::time::timeout(Duration::from_secs(3), ...)` and unwrapped with an informative `expect("test exceeded 3s timeout — semaphore serialization regressed?")`.

    Do NOT modify any `src/` file. Do NOT touch any existing test in `tests/`. This test does NOT need to import any rn-dash type — it directly tests the contract by constructing the same data structure that `src/app/effect_runner.rs` uses. This is a CONTRACT test, not an end-to-end test through Adapters (which would require excessive scaffolding). The risk is "what if effect_runner is wired wrong and never acquires the semaphore" — that risk is caught by the inline test in Plan 15-03 (`yarn_semaphores` field populated correctly) and by code review of the SpawnTask arm. This split is intentional per 15-RESEARCH §Validation Architecture (which says "substitute a slow fixture command and assert the SECOND task starts AFTER the first completes" — exactly what this test does).
  </action>
  <verify>
    <automated>cargo test --test yarn_semaphore_serializes --quiet</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/yarn_semaphore_serializes.rs` exists.
    - `grep -c 'Semaphore::new(1)' tests/yarn_semaphore_serializes.rs` returns at least 1.
    - `grep -c 'acquire_owned' tests/yarn_semaphore_serializes.rs` returns at least 1.
    - `grep -c 'canonicalize' tests/yarn_semaphore_serializes.rs` returns at least 1.
    - Both tests pass via `cargo test --test yarn_semaphore_serializes --quiet`.
    - Total wall-clock for both tests < 3 seconds (timeout-wrapped).
    - `tests/process_group_kill.rs` (COVER-02) and `tests/metro_single_instance.rs` (COVER-01) still pass unchanged.
    - `tests/process_group_cancel.rs` (Task 1) still passes.
    - `cargo clippy --all-targets -- -D warnings` clean.
    - `make arch-lint` — 21 G-XX guards green.
  </acceptance_criteria>
  <done>Same-repo-root serialization proven by 450ms+ gap between task starts; different-repo-roots parallelism proven by overlapping windows; semaphore contract characterized for any future refactor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration test → real OS subprocesses | Spawns bash sleep fixtures; signals delivered to real PGIDs |
| Test fixtures → CI runner | macOS / Linux POSIX assumed; gated by `#[cfg]` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06-01 | DoS | Test hangs forever if process group is not reaped or semaphore deadlocks | mitigate | All test bodies wrapped in `tokio::time::timeout(Duration::from_secs(2-3), ...)` with `.expect("timeout — regression in ...")`. CI will see a clear panic, not a hang. |
| T-15-06-02 | Tampering | Test pgid > 1 guard removed → kill(-1, ...) kills CI runner processes | mitigate | `assert!(pgid > 1)` Pitfall-3 guard copied from COVER-02 lines 56-63; mandatory inline in Task 1. |
| T-15-06-03 | Information disclosure | Real PIDs of CI runner subprocesses appear in test panic messages | accept | PIDs are ephemeral, machine-local, and only printed on test failure (debug output). Standard CI hygiene. |
| T-15-06-04 | Spoofing | Test passes against a buggy implementation because the fixture is too lenient | mitigate | Task 1 explicitly documents: "test fails red if `.process_group(0)` is removed from command_runner.rs" — verified by local mutation testing during plan execution. Task 2's same-vs-different-repo-root pair catches the "always serializes" and "never serializes" bugs symmetrically. |
| T-15-06-SC | Tampering | No new packages | — | N/A — only existing tokio/libc/std used. |
</threat_model>

<verification>
After both tasks:
- `cargo test --test process_group_cancel --quiet` — green
- `cargo test --test yarn_semaphore_serializes --quiet` — green
- `cargo test --test process_group_kill --quiet` — COVER-02 unchanged (D-22)
- `cargo test --test metro_single_instance --quiet` — COVER-01 unchanged (D-22)
- `cargo test --lib --quiet` — zero regressions from prior plans
- `cargo clippy --all-targets -- -D warnings` — clean
- `make arch-lint` — 21 guards green
- Wall-clock: all four integration tests combined complete in < 8 seconds
</verification>

<success_criteria>
- ROADMAP success criterion 1 PROVEN: orphan sleep grandchild reaped within 2 seconds via TokioCommandRunner + TokioTaskHandle::abort() — `cancel_via_task_handle_reaps_full_process_group` test.
- ROADMAP success criterion 4 PROVEN: same-repo-root serialization with 450ms+ gap, different-repo-root parallelism — `same_repo_root_serializes_two_yarn_family_tasks` + `different_repo_roots_run_in_parallel` tests.
- ROADMAP success criteria 2 + 3 already PROVEN by Plan 15-05's inline dispatch_tests sub-modules.
- All four ROADMAP success criteria now have green automated tests.
- D-22 invariant preserved: COVER-01 / COVER-02 never modified.
</success_criteria>

<output>
Create `.planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-06-SUMMARY.md` when done.
</output>
