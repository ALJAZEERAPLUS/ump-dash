---
phase: 15-task-cancellation-collision-shared-resource-semaphore
plan: 05
type: execute
wave: 4
depends_on: ["15-03", "15-04"]
files_modified:
  - src/app/update.rs
  - src/app/dispatch_tests.rs
autonomous: true
requirements:
  - TASK-04
  - TASK-05
tags: [update, collision-gate, is-cancellable-gate, dispatch, command-cancel]
must_haves:
  truths:
    - "Action::CommandCancel handler checks record.spec.is_cancellable() before calling record.handle.abort(); a non-cancellable task (any git porcelain variant) re-inserts its record into slice.task and does NOT clear the queue or push a [cancelled] output line — the cancel action is a no-op for non-cancellable variants"
    - "dispatch_command checks (discriminant, worktree_id) collision against slice.task BEFORE returning Effect::SpawnTask; if discriminants match, the collision_policy() predicate decides whether to drop the new dispatch (BlockNew) or cancel the existing task and dispatch the new one (CancelPrevious)"
    - "BlockNew path: dispatch_command returns None — no Effect emitted, slice.task unchanged"
    - "CancelPrevious path: existing task's handle.abort() called, slice.task cleared, slice.queue cleared, [cancelled by new dispatch] line appended to slice.output, then the new SpawnTask Effect is returned"
    - "The collision gate runs in dispatch_command which is called only from places that already established the worktree context — no cross-worktree side effects"
  artifacts:
    - path: src/app/update.rs
      provides: "is_cancellable() guard in CommandCancel + collision_policy() gate in dispatch_command"
      contains: "is_cancellable()"
    - path: src/app/dispatch_tests.rs
      provides: "New inline sub-modules `collision` and `cancellation_guard` with at least 4 new tests"
      contains: "mod collision"
  key_links:
    - from: "src/app/update.rs::Action::CommandCancel handler"
      to: "src/domain/command.rs::CommandSpec::is_cancellable"
      via: "if record.spec.is_cancellable() { record.handle.abort(); ... } else { slice.task = Some(record); }"
      pattern: "record\\.spec\\.is_cancellable\\(\\)"
    - from: "src/app/update.rs::dispatch_command"
      to: "src/domain/command.rs::CollisionPolicy"
      via: "match spec.collision_policy() { CollisionPolicy::BlockNew => return None, CollisionPolicy::CancelPrevious => { ... } }"
      pattern: "CollisionPolicy::(BlockNew|CancelPrevious)"
---

<objective>
Land the two TEA-level gates that consume the predicates from Plans 15-02
(via TokioTaskHandle::abort behaving correctly) and 15-04
(CollisionPolicy + collision_policy()): (1) the `is_cancellable()` guard in
the `Action::CommandCancel` handler that prevents `record.handle.abort()`
from firing SIGTERM at a running `git rebase` / `git push` / etc., and
(2) the `(discriminant, WorktreeId)` collision gate in `dispatch_command`
that consults `collision_policy()` and either drops the new dispatch
(BlockNew) or cancels the existing task and dispatches the new one
(CancelPrevious).

Purpose: This plan closes the last two `[ASSUMED]` items from Plan 14's
CommandCancel handler ("Phase 15 will add `is_cancellable()` guard" — comment
in 14-CONTEXT.md, 14-07-SUMMARY.md) and the F-501-deferred collision logic.
With this plan landed, ROADMAP success criterion 2 (git porcelain cannot be
cancelled) and ROADMAP success criterion 3 (collision per documented policy)
both become true at the TEA reducer level.

Output: ~50 LOC modified in `src/app/update.rs` (two handler bodies) and
~150 LOC added to `src/app/dispatch_tests.rs` (two new inline sub-modules).
No new files. No infra. No tokio. Pure TEA changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-RESEARCH.md
@.planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-PATTERNS.md
@.planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-03-SUMMARY.md
@.planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-04-SUMMARY.md
@src/app/update.rs
@src/app/dispatch_tests.rs
@src/domain/command.rs

<interfaces>
<!-- Contracts established by Plans 15-03 and 15-04 (DO NOT redefine — consume them): -->

From src/domain/command.rs (after Plan 15-04):
- `pub enum CollisionPolicy { BlockNew, CancelPrevious }`
- `impl CommandSpec { pub fn collision_policy(&self) -> CollisionPolicy { ... } }`
- `pub fn is_cancellable(&self) -> bool { ... }` (Phase 13 — unchanged)

From src/app/effect.rs (after Plan 15-03):
- `Effect::SpawnTask { task_id, worktree_id, spec, cwd, branch, repo_root }` — 6 fields, repo_root already populated by Plan 15-03 Task 1

Current Phase 14 + Plan 15-03 dispatch_command shape (src/app/update.rs lines 45-82 — verify after 15-03 lands; 15-03 added `repo_root: state.repo_root.clone()` to the SpawnTask construction):
- Returns `Some(Effect::SpawnTask { task_id, worktree_id, spec, cwd, branch, repo_root })`
- No collision gate yet — this plan adds it

Current Phase 14 CommandCancel handler shape (src/app/update.rs lines 673-689):
- `Action::CommandCancel => { ... if let Some(record) = slice.task.take() { record.handle.abort(); } slice.queue.clear(); slice.post_drain = None; slice.output.push_back("[cancelled]".into()); ... }`
- NO is_cancellable() check — this plan adds it

D-05 lock (Phase 14):
- Collision identity = `(std::mem::discriminant(&spec), worktree_id)`
- Two `YarnInstall` invocations collide; `YarnJest { filter: A }` and `YarnJest { filter: B }` collide because they share variant tag

D-08 lock (Phase 14):
- Stale CommandExited dropped: `CommandExited` handler looks up slice by task_id; if `slice.task` is None or has a different task_id, the action is silently dropped. This means the CancelPrevious path can safely abort the old task — its eventual CommandExited (Cancelled, from cancel_token arm, OR Killed from SIGKILL) arrives for a task_id that is no longer in the slice, gets dropped.

From src/app/dispatch_tests.rs (existing structure):
- `base_state()` helper produces a fresh AppState
- `seed_one_worktree(&mut state)` adds a worktree to state.worktree_browser.worktrees and state.worktrees
- `synthetic_task_record(id_value, spec)` builds a TaskRecord with a `NoopHandle` (no-op abort) — perfect for collision testing without real subprocesses
- Sub-modules follow the pattern `mod {topic} { use super::*; #[test] fn {name}() { ... } }`
- Phase 14's `command_queue` sub-module is the structural template (lines 574-658 of dispatch_tests.rs)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add is_cancellable() guard to Action::CommandCancel handler in update.rs</name>
    <files>src/app/update.rs</files>
  <read_first>
    - src/app/update.rs (lines 670-695 — the current CommandCancel handler body)
    - src/domain/command.rs (lines 119-137 — is_cancellable() predicate; this is the gate we consult)
    - .planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-PATTERNS.md (the `src/app/update.rs` section, "Phase 15 pattern — add is_cancellable() guard" code block at lines 485-500)
    - .planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-RESEARCH.md §Pitfall 5 (CommandCancel Without is_cancellable() Gate — danger of corrupting git rebase mid-flight)
  </read_first>
  <behavior>
    - Behavior 1: When `Action::CommandCancel` fires on a worktree slice whose `slice.task.as_ref().map(|t| t.spec.is_cancellable())` returns `Some(false)` (i.e., a running git-porcelain task), the handler is a NO-OP: slice.task remains unchanged, slice.queue is NOT cleared, slice.post_drain is NOT cleared, slice.output does NOT receive a "[cancelled]" line. No abort() call.
    - Behavior 2: When `Action::CommandCancel` fires on a slice whose running task IS cancellable (yarn/clean/rn/test/shell), the handler performs the full Phase 14 behavior: take the record, call abort(), clear queue + post_drain, push "[cancelled]" line.
    - Behavior 3: When `Action::CommandCancel` fires on a slice with no running task (`slice.task.is_none()`), the handler is also a no-op (Phase 14 behavior preserved — the `if let Some(record)` branch simply does not enter).
    - Behavior 4: A new `mod cancellation_guard` sub-module in `dispatch_tests.rs` (Task 2 of this plan) contains the inline tests for these three behaviors.
  </behavior>
  <action>
    Edit `src/app/update.rs`:
    1. Locate the `Action::CommandCancel` handler (currently lines 673-689). The current body is:
       ```
       Action::CommandCancel => {
           let active_id = active_worktree_id(state);
           if let Some(ref id) = active_id
               && let Some(slice) = state.worktrees.get_mut(id)
           {
               if let Some(record) = slice.task.take() {
                   record.handle.abort();
               }
               slice.queue.clear();
               slice.post_drain = None;
               slice.output.push_back("[cancelled]".into());
               if slice.output.len() > MAX_COMMAND_LINES {
                   slice.output.pop_front();
               }
           }
       }
       ```
    2. Rewrite the body so the cancellable-check wraps the take + abort + clear sequence. Target shape (from 15-PATTERNS.md):
       ```
       Action::CommandCancel => {
           let active_id = active_worktree_id(state);
           if let Some(ref id) = active_id
               && let Some(slice) = state.worktrees.get_mut(id)
           {
               if let Some(record) = slice.task.take() {
                   if record.spec.is_cancellable() {
                       record.handle.abort();
                       slice.queue.clear();
                       slice.post_drain = None;
                       slice.output.push_back("[cancelled]".into());
                       if slice.output.len() > MAX_COMMAND_LINES {
                           slice.output.pop_front();
                       }
                   } else {
                       // Phase 15 / 15-RESEARCH §Pitfall 5: git-porcelain variants must not
                       // receive SIGTERM mid-flight (data-integrity risk on git rebase / git push).
                       // CommandCancel is a no-op for them. Re-insert the record into the slice
                       // so the running task continues to completion.
                       slice.task = Some(record);
                   }
               }
           }
       }
       ```
       Note the take-then-maybe-reinsert pattern: we take() the record (which requires `&mut slice.task`), check is_cancellable() on the OWNED record, and either abort + clear OR put it back. This avoids needing a separate `slice.task.as_ref()` check that would require borrow-juggling.
    3. Verify the doc-comment block on the handler mentions the new gate. Add a one-liner: "Phase 15: is_cancellable() guard prevents SIGTERM at running git rebase/push (data-integrity)."

    Do NOT modify any other handler in this task. Do NOT touch `dispatch_tests.rs` in this task — Task 2 owns that. Do NOT touch `dispatch_command` — Task 3 of this plan owns the collision gate.
  </action>
  <verify>
    <automated>cargo build --quiet &amp;&amp; cargo test --lib --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'record.spec.is_cancellable()' src/app/update.rs` returns at least 1.
    - The take-then-reinsert pattern is present: `grep -c 'slice.task = Some(record);' src/app/update.rs` returns at least 1.
    - `cargo build --quiet` succeeds.
    - `cargo test --lib --quiet` reports zero regressions vs. the post-15-03/15-04 baseline. Existing Phase 14 tests that exercise the CommandCancel-on-yarn path still pass (the yarn path is cancellable, so behavior is unchanged for those tests).
    - `cargo clippy --all-targets -- -D warnings` clean.
    - `make arch-lint` — 21 G-XX guards green.
  </acceptance_criteria>
  <done>CommandCancel guards with is_cancellable(); non-cancellable variants no-op (record re-inserted); cancellable variants behave as Phase 14; no other handler touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add collision_policy() gate to dispatch_command in update.rs</name>
    <files>src/app/update.rs</files>
  <read_first>
    - src/app/update.rs (lines 45-82 — current dispatch_command body, AFTER Plan 15-03 added the repo_root field to SpawnTask)
    - src/domain/command.rs (CollisionPolicy + collision_policy() method added by Plan 15-04)
    - .planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-PATTERNS.md (the `src/app/update.rs` section, "Phase 15 collision gate pattern" at lines 519-541)
    - .planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-RESEARCH.md §F5 (Collision Identity) and §F6 (collision_policy() predicate consumer)
    - .planning/phases/14-per-worktree-task-system-foundation/14-CONTEXT.md (D-05 — discriminant-based identity)
  </read_first>
  <behavior>
    - Behavior 1: Before constructing the `Some(Effect::SpawnTask { ... })` return value, `dispatch_command` checks the (discriminant, worktree_id) collision against `slice.task`. If the slice has a running task and `std::mem::discriminant(&existing.spec) == std::mem::discriminant(&spec)`, the policy is consulted.
    - Behavior 2: Policy `CollisionPolicy::BlockNew`: `dispatch_command` returns `None` immediately. slice.task unchanged. slice.queue unchanged. slice.output unchanged. No Effect emitted. (The `$ argv` separator line written earlier in `dispatch_command` MUST also be skipped — the gate runs BEFORE the output line write. Restructure the function so the collision check runs before the slice.output.push_back of the `$ argv` line.)
    - Behavior 3: Policy `CollisionPolicy::CancelPrevious`: take the existing record from slice.task; call `record.handle.abort()`; clear slice.queue; push "[cancelled by new dispatch]" to slice.output (subject to MAX_COMMAND_LINES); then fall through to the normal dispatch path (write `$ argv` line, return `Some(Effect::SpawnTask { ... })` with the NEW task_id).
    - Behavior 4: When there is NO existing task in slice.task OR the discriminants do NOT match, dispatch_command behaves exactly as Phase 14 + Plan 15-03 (no change).
    - Behavior 5: The collision gate is the FIRST thing in dispatch_command after the worktree-selection step (lines 46-55) — verify with a read after edit.
  </behavior>
  <action>
    Edit `src/app/update.rs`:
    1. At the top of `update.rs` (in the `use` block — find existing `use crate::domain::command::...` lines), add `use crate::domain::command::CollisionPolicy;` if not already present (15-04 added it to domain/command.rs; this consumer needs the import). The existing import path may already include CommandSpec — extend the import line.
    2. Locate `dispatch_command` (line 45). After the worktree-selection step (after line 55 where `wt_id` is computed) and BEFORE the `let task_id = TaskId::next();` line, insert the collision gate. Target shape (adapted from 15-PATTERNS.md):
       ```
       // Phase 15 / TASK-05 collision gate (D-05, discriminant identity).
       // Before allocating a new TaskId or writing the $ argv line, check whether
       // an existing task on the same worktree shares the spec discriminant.
       let collision_decision: Option<()> = if let Some(slice) = state.worktrees.get(&wt_id) {
           if let Some(existing) = slice.task.as_ref() {
               if std::mem::discriminant(&existing.spec) == std::mem::discriminant(&spec) {
                   match spec.collision_policy() {
                       CollisionPolicy::BlockNew => return None,
                       CollisionPolicy::CancelPrevious => Some(()),  // signal: cancel + dispatch
                   }
               } else { None }
           } else { None }
       } else { None };
       // If collision_decision is Some(()), cancel the previous task with the [cancelled by new dispatch] message.
       if collision_decision.is_some() {
           if let Some(slice) = state.worktrees.get_mut(&wt_id) {
               if let Some(record) = slice.task.take() {
                   record.handle.abort();
               }
               slice.queue.clear();
               slice.output.push_back("[cancelled by new dispatch]".into());
               while slice.output.len() > MAX_COMMAND_LINES {
                   slice.output.pop_front();
               }
           }
       }
       ```
       Place this block AFTER the `let wt_id = wt.id.clone();` line and BEFORE `let task_id = crate::domain::task::TaskId::next();`. The two-pass approach (immutable borrow to read existing.spec discriminant, then mutable borrow to take + abort) avoids the borrow-checker conflict that would arise from holding `slice` mutably while calling `record.handle.abort()` (which is `&self`, but the compiler can't see that across the take).
    3. Verify the rest of `dispatch_command` (the `$ argv` line write, the `Effect::SpawnTask` construction with `repo_root` from 15-03) is unchanged — the collision gate is a pure prefix.
    4. Update the doc comment block above `dispatch_command` (currently lines 38-44) to mention the Phase 15 collision gate.

    Do NOT touch `Action::CommandCancel` (Task 1 of this plan owns it). Do NOT touch dispatch_tests.rs (Task 3 owns it).
  </action>
  <verify>
    <automated>cargo build --quiet &amp;&amp; cargo test --lib --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CollisionPolicy::BlockNew => return None' src/app/update.rs` returns 1.
    - `grep -c 'CollisionPolicy::CancelPrevious' src/app/update.rs` returns at least 1.
    - `grep -c 'std::mem::discriminant' src/app/update.rs` returns at least 1.
    - `grep -c '\[cancelled by new dispatch\]' src/app/update.rs` returns 1.
    - `cargo build --quiet` succeeds.
    - `cargo test --lib --quiet` zero regressions vs. post-Task-1 baseline. Existing dispatch tests that dispatch a single command on an empty slice are unaffected (no collision).
    - `cargo clippy --all-targets -- -D warnings` clean.
    - `make arch-lint` — 21 G-XX guards green.
  </acceptance_criteria>
  <done>dispatch_command consults collision_policy when discriminants match; BlockNew returns None; CancelPrevious aborts previous + clears queue + pushes [cancelled by new dispatch] before falling through to normal dispatch; rest of function unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add `mod collision` and `mod cancellation_guard` sub-modules to dispatch_tests.rs with ≥5 inline tests</name>
    <files>src/app/dispatch_tests.rs</files>
  <read_first>
    - src/app/dispatch_tests.rs (entire file structure; especially lines 140-150 for the `synthetic_task_record` helper, lines 574-658 for the `command_queue` sub-module structural template, and the top of the file for `base_state()` + `seed_one_worktree()` helpers)
    - .planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-PATTERNS.md (the `src/app/dispatch_tests.rs` section, "New sub-modules to add" at lines 609-627)
    - src/domain/command.rs (the new CollisionPolicy + collision_policy() from Plan 15-04 — confirm spec → policy mapping)
  </read_first>
  <behavior>
    Five inline tests across two new sub-modules:

    **`mod collision`:**
    - `collision_block_new_yarn_install_drops_new_dispatch`: seed worktree; insert a synthetic `TaskRecord` with `spec = CommandSpec::YarnInstall` into slice.task; call `update(&mut state, Action::CommandRun(CommandSpec::YarnInstall))`; assert (a) the returned effects vec contains NO `Effect::SpawnTask`, (b) slice.task is STILL Some with the original task_id, (c) slice.output is unchanged (no `$ yarn install` line written).
    - `collision_cancel_previous_yarn_jest_replaces_task`: seed worktree; insert synthetic TaskRecord with `spec = CommandSpec::YarnJest { filter: "first".into() }`; call `update(&mut state, Action::CommandRun(CommandSpec::YarnJest { filter: "second".into() }))`; assert (a) the returned effects vec contains exactly one `Effect::SpawnTask` with `spec = CommandSpec::YarnJest { filter: "second" }`, (b) slice.task is None (the original was taken and aborted; the NEW one is delivered via task_handle_tx asynchronously, not visible to the synchronous update() return), (c) slice.output contains "[cancelled by new dispatch]".
    - `collision_different_discriminants_dispatch_normally`: seed worktree; insert TaskRecord with `spec = CommandSpec::YarnInstall`; dispatch `CommandSpec::YarnJest { filter: "x".into() }`; assert the existing task STAYS in slice.task (different discriminant — no collision) AND the new dispatch returns `Effect::SpawnTask` (queued or dispatched depending on existing dispatch_command logic; the test asserts at minimum that the existing YarnInstall TaskRecord is untouched and no `[cancelled by new dispatch]` line is written).
    - `collision_git_pull_block_new`: seed worktree; insert TaskRecord with `spec = CommandSpec::GitPull`; dispatch another `CommandSpec::GitPull`; assert (a) effects vec contains NO new SpawnTask, (b) slice.task is STILL Some (the original), (c) slice.output is unchanged. This is the Q-4 honor test: git → BlockNew.

    **`mod cancellation_guard`:**
    - `cancel_on_git_pull_is_noop_record_reinserted`: seed worktree; insert TaskRecord with `spec = CommandSpec::GitPull`; call `update(&mut state, Action::CommandCancel)`; assert (a) slice.task is STILL Some with the SAME task_id (record was re-inserted), (b) slice.queue is unchanged (not cleared), (c) slice.output does NOT contain "[cancelled]".
    - `cancel_on_yarn_install_aborts_and_clears`: seed worktree; insert TaskRecord with `spec = CommandSpec::YarnInstall`; pre-load slice.queue with a follow-up `CommandSpec::YarnLint`; call `update(&mut state, Action::CommandCancel)`; assert (a) slice.task is None (taken), (b) slice.queue is EMPTY (cleared), (c) slice.output contains "[cancelled]".
  </behavior>
  <action>
    Edit `src/app/dispatch_tests.rs`:
    1. Append two new sub-modules at the END of the file, after the existing last sub-module. Use the structural template from `mod command_queue` (lines 574-658). Each sub-module starts with `use super::*;`.
    2. For each test, use the existing helpers: `let mut state = base_state();`, `seed_one_worktree(&mut state);`, `let wt_id = ...` (read from state.worktree_browser.worktrees[0].id.clone() or whatever the existing convention is — check the existing tests for the exact pattern). Insert TaskRecords via `state.worktrees.get_mut(&wt_id).unwrap().task = Some(synthetic_task_record(N, spec));`. The `synthetic_task_record(id_value, spec)` helper at lines 140-150 builds a TaskRecord with a `NoopHandle` whose `abort()` is a no-op — perfect; the test does not need to verify abort() side effects (Plan 15-06's integration test owns the real-process kill assertion).
    3. For each test, run `let effects = update(&mut state, Action::CommandRun(spec))` or `let effects = update(&mut state, Action::CommandCancel)`. Inspect the returned `effects: Vec<Effect>` to assert presence/absence of `Effect::SpawnTask`.
    4. Use `matches!` and explicit destructure to assert effect shape:
       ```
       let has_spawn = effects.iter().any(|e| matches!(e, Effect::SpawnTask { spec: CommandSpec::YarnJest { filter }, .. } if filter == "second"));
       assert!(has_spawn, "expected Effect::SpawnTask for the new YarnJest dispatch");
       ```
    5. For state assertions, use `state.worktrees.get(&wt_id).unwrap()` to read back the slice.
    6. Each test must complete in `< 100ms` (no tokio runtime needed — pure update() reducer calls).

    Do NOT modify any existing test. Do NOT introduce new helpers if existing ones suffice. Do NOT touch `update.rs` or `command.rs`.
  </action>
  <verify>
    <automated>cargo test --lib --quiet dispatch_tests::collision dispatch_tests::cancellation_guard &amp;&amp; cargo test --lib --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'mod collision' src/app/dispatch_tests.rs` returns 1.
    - `grep -c 'mod cancellation_guard' src/app/dispatch_tests.rs` returns 1.
    - All 6 new tests pass (4 in mod collision, 2 in mod cancellation_guard).
    - All 22+ existing dispatch tests still pass.
    - `cargo test --lib --quiet` reports net +6 tests vs. post-Task-2 baseline.
    - `cargo clippy --all-targets -- -D warnings` clean.
    - `make arch-lint` — 21 G-XX guards green.
    - `tests/process_group_kill.rs` and `tests/metro_single_instance.rs` still pass unchanged (D-22).
  </acceptance_criteria>
  <done>Six new inline tests across two new sub-modules cover the four collision-policy paths (block-new yarn install, cancel-previous yarn jest, different-discriminant no-op, block-new git pull) and the two cancellation-guard paths (git pull no-op, yarn install full abort).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Action::CommandCancel → record.handle.abort() | TEA reducer call into infra-owned handle; now gated by domain predicate |
| Action::CommandRun → dispatch_command → state mutation | TEA reducer call; now gated by collision_policy() decision tree |
| domain predicates (is_cancellable, collision_policy) → app/update | Pure data dependency — no I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05-01 | Tampering | SIGTERM sent to running `git rebase` mid-operation, corrupting MERGE_HEAD / refs / index | mitigate | is_cancellable() guard in CommandCancel handler refuses to call abort() on any git variant. Re-insert pattern ensures the running git task continues to completion. 15-RESEARCH §Pitfall 5. Verified by `cancel_on_git_pull_is_noop_record_reinserted` test. |
| T-15-05-02 | Repudiation | User triggers CommandCancel on a git task expecting it to stop, but the no-op is invisible | accept | The output panel will NOT show "[cancelled]" — the user sees the git command continue. Acceptable for ASVS L1 / TUI app; future UX phase could add a warning toast. |
| T-15-05-03 | DoS | Repeated YarnInstall dispatches in a tight loop while one is running could DoS the user's intent | mitigate | BlockNew silently drops the new dispatch — no slot allocation, no Effect emitted, no queue buildup. Idempotent by design. Verified by `collision_block_new_yarn_install_drops_new_dispatch` test. |
| T-15-05-04 | Race | CancelPrevious takes the record + clears queue, then the NEW dispatch path runs — between these two steps, an external CommandExited for the old task_id could arrive | accept | D-08 stale-task-drop: CommandExited looks up slice.task by task_id; if task_id no longer matches (because we took + new dispatch will populate with new task_id via task_handle_tx), the action is silently dropped. Plan 14-07's slice-local CommandExited handler handles this race. |
| T-15-05-SC | Tampering | No new packages | — | N/A — pure TEA-reducer change. |
</threat_model>

<verification>
After all three tasks:
- `cargo build --quiet` — green
- `cargo test --lib --quiet` — +6 new tests, zero regressions
- `cargo test --test process_group_kill --quiet` — COVER-02 unchanged
- `cargo test --test metro_single_instance --quiet` — COVER-01 unchanged
- `cargo clippy --all-targets -- -D warnings` — clean
- `make arch-lint` — 21 guards green
- ROADMAP success criterion 2 verified by `cancel_on_git_pull_is_noop_record_reinserted`
- ROADMAP success criterion 3 verified by all 4 `mod collision` tests
- ROADMAP success criterion 1 (timing within 2s) and 4 (semaphore serialization) remain Plan 15-06's integration-test verification scope
</verification>

<success_criteria>
- TASK-04 + TASK-05 fully closed at the TEA-reducer level: every cancellation goes through the is_cancellable() gate; every dispatch checks for (discriminant, worktree_id) collision and consults collision_policy().
- All four ROADMAP success criteria for Phase 15 either fully met (criterion 2 + 3) or mechanically prepared for integration-test verification (criterion 1 + 4).
- D-22 invariant preserved.
- Wave 5 unblocked: Plan 15-06 (integration tests) can now spawn real subprocesses and assert the end-to-end timing / serialization.
</success_criteria>

<output>
Create `.planning/phases/15-task-cancellation-collision-shared-resource-semaphore/15-05-SUMMARY.md` when done.
</output>
