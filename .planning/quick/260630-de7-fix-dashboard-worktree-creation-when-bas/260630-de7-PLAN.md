---
status: ready
quick_id: 260630-de7
slug: fix-dashboard-worktree-creation-when-bas
description: fix dashboard worktree creation when base branch is local-only and not on origin
---

# Quick Task 260630-de7: Local-only worktree base branches

## Task 1 - Reproduce the bug at the infra seam

Files:
- `src/infra/worktrees.rs`

Action:
- Add a focused async regression test that creates a temporary git repository with a committed local branch that has no `origin/<branch>` ref.
- Call `add_worktree_new_branch(repo, "branch-test", "UMP-6831")`.
- Assert the worktree path is created.

Verify:
- Run the focused test and confirm it fails before the implementation because the command tries `origin/UMP-6831`.

Done:
- The test fails for the reported invalid reference behavior, not for setup or unrelated git configuration.

## Task 2 - Resolve the correct git start point

Files:
- `src/infra/worktrees.rs`
- `src/domain/ports/worktree_port.rs`

Action:
- Update `add_worktree_new_branch` to use `refs/remotes/origin/<base>` when that ref exists.
- Fall back to `refs/heads/<base>` when the base branch is local-only.
- Keep the concrete git probing inside infra.
- Update the domain port comment so it no longer promises remote-only behavior.

Verify:
- Run the focused regression test.
- Run `make arch-lint`.
- Run `cargo test`.

Done:
- New worktree creation works from local-only base branches while preserving remote-branch behavior.
