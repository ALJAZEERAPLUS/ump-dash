---
quick_id: 260529-gv7
slug: remove-the-prompt-that-opens-when-openin
status: complete
---

# Quick Task 260529-gv7: Remove Claude tab suffix prompt

## Goal

Opening Claude Code from the worktree list should immediately open a Claude tab/window with the standard `<worktree-prefix>-claude` name. The custom suffix text-input prompt is no longer needed.

## Tasks

1. Add an update-level regression test covering `Action::OpenClaudeCode`.
   - Verify a selected worktree produces an `Effect::OpenInMultiplexer`.
   - Verify the effect name uses the existing `-claude` suffix.
   - Verify no modal or pending Claude prompt state remains.

2. Remove the Claude suffix prompt flow.
   - Replace the `OpenClaudeCode` text-input modal path with direct effect creation.
   - Remove now-unused `pending_claude_open` state and submit handling.

3. Verify with the focused Rust test and a broader cargo test pass if feasible.
