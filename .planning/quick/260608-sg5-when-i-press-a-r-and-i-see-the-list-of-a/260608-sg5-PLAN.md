---
status: in_progress
quick_id: 260608-sg5
date: 2026-06-08
---

# Quick Task 260608-sg5: Android physical devices in run picker

## Goal

Show connected physical Android devices in the `a r` run target picker alongside emulator targets when `adb devices -l` reports them.

## Tasks

1. Lock the Android ADB parser regression.
   - Files: `src/infra/devices.rs`
   - Action: add a focused unit test proving a space-separated physical-device row from `adb devices -l` is parsed.
   - Verify: the focused Cargo test fails before implementation and passes after implementation.

2. Relax Android device parsing.
   - Files: `src/infra/devices.rs`
   - Action: parse `adb devices -l` rows using whitespace tokens instead of requiring a tab between serial and state, while still skipping non-`device` states.
   - Verify: run the focused parser test plus the existing device tests.
