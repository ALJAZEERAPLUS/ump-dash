---
quick_id: 260622-n7p
slug: android-build-doesn-t-work-console-shows
status: in_progress
created: 2026-06-22
---

# Quick Task 260622-n7p: Fix Android build/run success without install

Diagnose and fix the Android flow where the dashboard reports success and opens an emulator, but the app is not installed or launched.

## Tasks

### 1. Reproduce the queued Android flow

files: `src/app/dispatch_tests.rs`, `src/app/update.rs`, `src/domain/command.rs`

action: Add or run a focused regression around AVD boot success draining into the actual Android run/install path. Confirm whether the queued UMP run, release install, or cached launch path is being skipped.

verify: The failing signal matches the reported symptom: a successful preliminary command leaves no install/run effect or command.

done: Root cause is identified from a deterministic test or a documented inability to reproduce locally.

### 2. Patch the smallest affected path

files: expected `src/app/update.rs` and/or `src/domain/command.rs`

action: Change only the dispatch/command construction needed so a successful Android build/run path continues into install and launch.

verify: The regression test passes and existing Android dispatch tests still pass.

done: The dashboard no longer treats the preliminary Android step as the whole operation.

### 3. Verify and record

files: `.planning/STATE.md`, `.planning/quick/260622-n7p-android-build-doesn-t-work-console-shows/260622-n7p-SUMMARY.md`

action: Run focused Cargo tests, architecture guard, and complete the quick-task summary/state row.

verify: `cargo test` focused targets pass; `make arch-lint` passes or any failure is documented.

done: Code and GSD artifacts are committed atomically without staging unrelated user files.
