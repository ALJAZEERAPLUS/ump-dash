---
quick_id: 260626-gp1
slug: carefully-add-ability-to-mcp-to-modify-w
status: complete
created: 2026-06-26
---

# Quick Task 260626-gp1: carefully add ability to MCP to modify worktrees create/delete

## Scope

Add MCP tools that let agents create and delete dashboard-managed git worktrees through the existing dashboard action pipeline.

## Plan

1. Extend the domain agent protocol with create/delete worktree requests and a worktree-operation started outcome.
2. Route those requests through `update()` so MCP calls reuse app-owned worktree effects and guards.
3. Expose `create_worktree` and `delete_worktree` rmcp tools and update seeded agent guidance.
4. Cover create/delete dispatch, confirmation guards, main-worktree deletion refusal, and protocol JSON round trips.

## Verification

- `cargo test --lib agent_requests` red before implementation, then green after.
- `cargo test`
- `cargo clippy --all-targets -- -D warnings`
- `make arch-lint`
- `make arch-report`
