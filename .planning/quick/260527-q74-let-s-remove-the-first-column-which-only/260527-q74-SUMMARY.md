---
quick_id: 260527-q74
status: complete
commit: 7fd6cdd
date: 2026-05-27
files_changed: 1
---

# Quick Task 260527-q74 Summary

**Done.** Dropped `.highlight_symbol("> ")` from the worktree table in
`render_worktree_table` (`src/ui/panels.rs:218`). Selection now conveyed
by `row_highlight_style` background only — no left gutter on any row.

## Verification

- `cargo build`: clean, no errors/warnings.
- Selected row distinguishes via bg (DarkGray normally, dark green when
  metro running) per `highlight_style` block at `panels.rs:196-204`.
- No `highlight_symbol` call remaining in `render_worktree_table`.

## Commit

`7fd6cdd` style(ui): drop highlight_symbol gutter from worktree table
