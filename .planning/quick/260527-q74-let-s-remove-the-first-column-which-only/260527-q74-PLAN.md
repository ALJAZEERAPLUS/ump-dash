---
quick_id: 260527-q74
description: Remove the first column that only highlights the current row
date: 2026-05-27
mode: quick
---

# Quick Task 260527-q74: Remove highlight-symbol gutter from worktree table

## Goal

Drop the leftmost gutter column on the worktree table. The gutter renders `> `
on the currently selected row and a blank on every other row — it exists
purely to mark selection. Row background highlight (`row_highlight_style`)
already conveys selection, so the gutter is redundant and steals 2 cells
from every row.

## Tasks

### 1. Remove `.highlight_symbol("> ")` from the `render_worktree_table` Table builder

- **Files:** `src/ui/panels.rs`
- **Action:** Delete the `.highlight_symbol("> ")` line at the end of the
  `Table::new(...).block(block).row_highlight_style(highlight_style)`
  chain. Replace with a short comment explaining why no symbol is set.
- **Verify:** `cargo build` clean; selected row in the TUI shows only the
  background highlight (no `> ` prefix); idle rows have no left gutter.
- **Done:** Edit landed, build passes, visual confirmed in `cargo run`.

## must_haves

- truths:
  - "Worktree table reclaims 2 cells of left margin (no `> ` gutter on any row)"
  - "Selected row still visually distinct via `row_highlight_style` bg"
- artifacts:
  - "`src/ui/panels.rs` — no `highlight_symbol` call in `render_worktree_table`"
