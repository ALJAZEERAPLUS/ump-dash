---
quick_id: 260529-fs9
slug: let-s-add-support-for-ghostty-the-same-w
status: planned
created: 2026-05-29
---

# Quick Task 260529-fs9: Add Ghostty terminal support

## Goal

Support Ghostty anywhere the app currently supports opening worktree commands in tmux or zellij.

## Tasks

1. Add focused multiplexer tests
   - Files: `src/infra/multiplexer.rs`
   - Action: Cover Ghostty environment detection and Ghostty command construction without spawning external terminals.
   - Verify: Run the new targeted test and confirm it fails before implementation.
   - Done: Tests prove `GHOSTTY_RESOURCES_DIR` selects Ghostty after tmux/zellij priority.

2. Implement Ghostty adapter
   - Files: `src/infra/multiplexer.rs`
   - Action: Add `GhosttyAdapter`, detect it from Ghostty's documented environment marker, and build the appropriate launch command.
   - Verify: Targeted tests pass.
   - Done: `detect_multiplexer()` returns Ghostty when running in Ghostty outside tmux/zellij.

3. Update user-facing copy and docs
   - Files: `src/app/update.rs`, `src/app/keybindings.rs`, `src/app/adapters.rs`, `README.md`
   - Action: Replace tmux/zellij-only text with tmux/zellij/Ghostty wording where the feature is described.
   - Verify: `cargo test` passes.
   - Done: No user-facing support text omits Ghostty for this feature.
