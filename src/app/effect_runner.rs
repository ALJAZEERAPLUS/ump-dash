//! Plan 13-08 populates this file. `EffectRunner` will consume `Vec<Effect>`
//! from `update()` (once F-201 lands in Plan 13-07) and dispatch each effect
//! via the `Adapters` struct.
#![allow(dead_code)]

pub struct EffectRunner;
